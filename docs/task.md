# Tasks

The execution plan: the build order of [architecture.md](architecture.md) §15.4, sliced into
sequential tasks. Task *n+1* assumes task *n* is done. Tasks are **vertical slices in build
order, not per-application backlogs**: where a feature spans back and front, the API task comes
immediately before its screen task, so the sequence stays linear and every screen lands against
an API that already exists. Front and back are combined into one task only where splitting them
would produce fragments. The **Scope** column names the workspaces a task touches (`api`, `web`,
`admin`, `worker`, `pkg:*`, `infra`, `config`, `docs`, `root`); the UI track additionally
follows `design/IMPLEMENTATION_PLAN.md` on this same order.

**This is a tracking artifact, not an eighth specification document.** The seven-document set
keeps its precedence: where a task row and a spec identifier disagree, the identifier wins and
this row is what is wrong. Cite decisions by identifier (`FR-…`, `AD-…`, `DR-…`) rather than
restating them.

**Status:** `DONE` · `IN PROGRESS` · `TODO` · `BLOCKED`. Task numbers are stable — cite them in
commits and in [build-log.md](build-log.md), which records *how* each completed task actually
went, deviations included.

**The Status cell holds exactly one of those four words — nothing else, and no date**; when a task
closed is the date on its [build-log.md](build-log.md) entry. A parent row's status is the roll-up
of the sub-steps beneath it. The rule, and why it is written down rather than left to convention, is
in the root `CLAUDE.md` beside this file's own entry in the document table.

**A number is an identifier; only tasks 1–73 are also the order.** Those were sliced together and
each assumes its predecessor. Anything added afterwards is **appended, never inserted**: a task
number is cited in commit messages, in `architecture.md`, in `build-log.md`, in nine migration
files and in source comments, so renumbering would falsify every one of them and leave the stale
copies in git history wrong forever. The cost is that in the appended section the document's order
and the number's order differ — so each of those rows states where it actually lands, and a row
that must precede an earlier number says so in bold.

**A task too large to close in one go is split into sub-steps, never into new numbers.** Task 25
becomes `25.1`, `25.2`, `25.3`; the numbered row above them keeps its name, its description and its
recorded deferrals **verbatim**, and *task 25* still names the same body of work — so every citation
of it in `CLAUDE.md`, [build-log.md](build-log.md), `config/seed/README.md`, the migrations and some
forty source comments stays true. A sub-step is a **new identifier**, not a renumbering of anything,
which is the only reason the paragraph above permits it.

**Reading a split row.** A **bold number** is a group: its Description and Expected-result columns
are the original row, unchanged, and its Status is the roll-up of the sub-steps below it. A decimal
number is the executable unit — the thing someone picks up, closes, and writes a build-log entry
for. Where a sub-step's Scope differs from its parent's, the row says so and says why; that
correction is task 23's, made in its unknowns batch rather than discovered mid-build.

**A sub-step may be renumbered while nothing frozen cites it — and the renumbering is recorded.** Amended 25 Aug 2026, the first time it bit: task 36 went from four module slices to eleven, which shifted `36.3` … `36.7`. The rule above protects *history* — commit messages, migrations, source comments — and none of those named a `36.x`; the only citation was one row in this file, fixed in the same edit. Appending B4 as `36.8` would have obeyed the letter and destroyed the reading order of a task nobody has started. **The test is what a number is frozen into, not how old it is:** `25.4`, `51.1`, `51.4`, `52.3` and `60.4` are cited in commit `89781d7` and can no longer move, and a sub-step named in a build-log entry never moves again.

**A sub-step is sized to close on its own.** It passes `pnpm gates` and earns its own build-log
entry without waiting for its siblings — that is the whole test. A slice that cannot (a migration
without the use case that reads it, a screen without its API) is not a sub-step but half of one, and
belongs merged with its other half. Where a sub-step's unknowns differ from its parent's, they are
named in its row and raised in its own batch.

**Split 25 Aug 2026, and the evidence was already in the build log.** Task 23 shipped and then
needed **three addenda**; tasks 20 and 24 needed one each. An addendum is a task that was declared
closed and was not, and every one of them landed on a row carrying three or more independent
deliverables. Tasks 1–24 are left exactly as they were: they are `DONE`, and their history is what
it is.

**Before starting a task, raise its unknowns in one batch** (CLAUDE.md, "Open questions are not
debt"). A task is not started by writing its first file; it is started by checking what it needs
decided.

**Before closing one, run `pnpm gates` — and `pnpm gates:clean` before pushing** (CLAUDE.md,
"Closing a task"). The clean run is what catches a gate depending on state a previous command left
behind, which an incrementally-built tree hides completely. Then write the build-log entry: a task
whose reasons are not recorded is not closed.

---

## Phase 1 — Foundation (§15.4 #1)

| # | Name | Scope | Description | Expected result (deliverables) | Status |
| --- | --- | --- | --- | --- | --- |
| 1 | Specification set | docs | Author the seven-document set with its identifier registers; resolve the initial open questions and record the amendments (three locales, WCAG 2.2 AA — followed but unratified, `design_spec` OQ-1 / `architecture` OQ-3) | `docs/` complete: FR-1…173, NFR-1…93, UC-01…176, AD/DR/UX registers, live open-question registers in every doc | DONE |
| 2 | Monorepo bootstrap and gates | root | pnpm workspace with the §12 pin table as a `catalog:` (`catalogMode: strict`, `saveExact`), `strictDepBuilds` + adjudicated `allowBuilds`, `engineStrict`, the `only-allow pnpm` guard; root gate scripts | Working `pnpm lint / typecheck / build / test / boundaries / boundaries:prove / openapi:check / routes:check`; 20 boundary rules, each with a fixture proving it rejects a real violation | DONE |
| 3 | API application scaffold | api | NestJS module tree (35 modules registered, empty) split by bounded context (DR-1), response envelope, problem+json filter, `TenantRepository` port surface, OpenAPI emission (DR-11) | App boots with a health controller; `packages/contracts/openapi/v1.json` emitted and diffed by `openapi:check` (zero paths yet, but the gate exists before the first path) | DONE |
| 4 | Web application scaffold | web | 36 route files across four route groups, next-intl wiring, session proxy; every page returns `null` | Route tree covering the S-01…S-28 tenant screens; `routes:check` green | DONE |
| 5 | Admin application scaffold | admin | 26 route files covering A-01…A-18, two pathless layouts, TanStack Router + Query, 15 feature folders split platform/billing (`features/core` absent — that absence is D-5) | Admin route tree and context split under the boundary gates | DONE |
| 6 | i18n wiring | pkg:i18n | Locale registry, message-loader port, fallback reporter, +40% expansion harness, wired across api, web and admin; message catalogues ship in the release, only store-edited copy stays in config (architecture.md OQ-43) | Three separately-authored locale catalogues load in all three apps; expansion harness runnable per phase | DONE |
| 7 | Dev Compose stack | infra | PostgreSQL 18 and Redis as Compose services, plus the database roles RLS needs; `pnpm dev:up` / `dev:down`; no host installs (architecture.md §12.5.10) | Working local stack; `psql` reached through the container only | DONE |
| 8 | Auth and UI dependency pins | root | Pin the auth stack (Argon2id, passport) and UI primitives (Radix, Lucide) through the catalog, recording version and verification date in §12 | Catalog entries resolving cleanly; §12 rows carry the verification date | DONE |
| 9 | Migration runner and datasource | api | TypeORM 1.1 datasource with `synchronize` off; the SQL migration runner and its gate-set script; a baseline migration applying cleanly on the Compose stack | Migration run/revert wired into the gate set; the baseline applies from an empty database | DONE |
| 10 | Core and billing schemas | api | `core` and `billing` as separate PG schemas with no cross-schema FK (DR-1, NFR-15, T-2); column conventions fixed at the base — `timestamptz` instants converted to epoch-ms at the DTO boundary (OQ-50, closed 19 Aug 2026), calendar date + originating timezone for legal dates (NFR-34) | Both schemas created by migration; no FK crosses them; the time conventions visible in the first tables | DONE |
| 11 | Tenant context propagation | api | Tenant context carried from the request into the DB session setting; `TenantRepository` throws without context (T-11's mitigation). §6.2's "TenantContextInterceptor" is a **guard** — NestJS runs all guards before any interceptor, and `EntitlementGuard` needs the binding (§6.2, amended 19 Aug 2026) | A context-less query throws; context flows through a request e2e | DONE |
| 12 | RLS policies and isolation proof | api | RLS policies on every tenant-scoped table (DR-5, AD-2), written against the Compose role split — not a superuser connection RLS silently exempts, and with `FORCE ROW LEVEL SECURITY`, since `esg_migrator` owns every table and an owner is exempt from its own policies regardless of `rolbypassrls` (§7.6). AD-2's stated policy form amended 20 Aug 2026: the tenant root is scoped by `id`, and its `INSERT` carries `WITH CHECK (true)` so FR-13 can create an organization | Policies live in SQL migrations; a test proves a cross-tenant read returns nothing | DONE |
| 13 | Append-only substrate | api | The append-only mechanism — `audit.reject_mutation()` and `audit.enforce_append_only(regclass)` — plus `audit.system_audit_log`, partitioned, as its first table (DR-6, NFR-33, §7.7). Ledger, metering and support-access tables apply the same procedure in tasks 61, 62 and 67; the gate fails any audit table that skips it | `REVOKE`-based migrations; a test proves `UPDATE`/`DELETE` are denied at the DB | DONE |
| 14 | Per-field audit capture | api | Per-field change captured by a **database trigger** onto `core.field_change` (P-11, FR-54, FR-55) — AD-14 constraint 5 amended 20 Aug 2026, since a function must be called and a trigger cannot be bypassed. `system_audit_log.organization_id` relaxed to nullable for FR-80's platform events | Mutations produce per-field audit rows automatically | DONE |
| 15 | Transactional outbox | api+worker | `audit.outbox_event`; a state change and its outbox row commit together (AD-6, P-8); the worker dispatcher enqueueing to AD-10's single BullMQ queue — enqueue **before** marking dispatched, so delivery is at-least-once, with the idempotency key as the job id for effectively-once processing (T-5) | Dispatcher on the worker entrypoint; redelivery-after-crash test passes | DONE |
| 16 | Configuration store | api+config | The config-as-data substrate (DR-3, AD-4): **one generic store**, artefacts as data — `config.entry_version` (immutable, all states) plus `config.entry_schedule` (what is in force, carrying §7.9's `WITHOUT OVERLAPS` key), a store-version counter bumped by trigger, and the ≤5 s replica poll. Seeded idempotently from `config/seed`. Taxonomy, factor sets, rules, plans and notification behaviour are rows added by tasks 33, 37, 40, 49 and 53 — no table and no code per artefact | A config change takes effect with no redeploy and reverts in one step | DONE |
| 17 | CI gate workflow | infra | The root gate set enforced on every push, in two parallel jobs split on whether Docker is needed. Workflows in `.github/workflows/` (the only place GitHub reads them); `infra/ci/setup` holds the composite action every job shares. The stack is `pnpm dev:up` rather than `services:` containers, so `init.sh`'s four roles are not reimplemented in YAML (§12.5.4, clarified 20 Aug 2026) | Green pipeline running all nine gates | DONE |
| 18 | CI images and billing-off job | infra | `apps/{api,web,admin}/Dockerfile`, root build context. **`pnpm deploy` is not usable on pnpm 11** — see CLAUDE.md and §10.4; a lockfile-driven `--prod` prune plus a copy of the directories pnpm's relative links span replaces it. `api` and `worker` are one image (AD-1). `renderer` and its Chromium install move to task 44, where the PDF pipeline exists. CI builds all three, **runs** each, and adds the `BILLING_ENABLED=false` job (T-1, NFR-1) whose `billing-disabled.e2e` asserts the opposite of the billing-on job — neither means anything alone | Three images build in CI; the billing-off job exists and grows the UC-17…48 suite as it lands | DONE |

## Phase 2 — Identity (§15.4 #2)

| # | Name | Scope | Description | Expected result (deliverables) | Status |
| --- | --- | --- | --- | --- | --- |
| 19 | Registration and verification API | api | Email + password registration (Argon2id), verification token issue/consume, dispatch through a minimal mail port — task 49's notification system absorbs the port later; it must not wait for it (P-7). Four values the sources never stated are closed here: password policy (OQ-51), FR-3's unverified window (OQ-52), whether registration answers uniformly (OQ-53) and whether a raw token may travel in the outbox payload (OQ-54) | Signup → verify e2e at the API; no vendor type outside the mail adapter | DONE |
| 20 | Registration and verification screens | web | The sign-up and verification screens per design_spec, three locales | A user registers and verifies from the browser; expansion harness passes on the new screens | DONE |
| 21 | Sessions and sign-in API | api | Session issuance, expiry and sign-out; one public API serving both front ends (DR-11, AD-9). **Includes FR-6 password reset** (architecture.md OQ-56, 21 Aug 2026): the reset link is the only lockout release before Phase 8, its consumption invalidates sessions this task creates, and task 22's S-02 reset screens need its API. Three values closed in this task's batch: tenant session lifetime (OQ-35 — 7 d idle / 30 d absolute), FR-6/FR-7 placement (OQ-56), the unverified-with-correct-password answer (OQ-57) | Sign-in/out e2e; expiry honoured; lockout at 10 consecutive failures released by reset; reset invalidates all sessions | DONE |
| 22 | Web sign-in and session proxy | web | The sign-in screens and the session proxy wired end-to-end — the only place a browser credential is exchanged. OQ-33 closed in this task's batch (sealed `SameSite=Lax` cookie + same-origin proof, §12.5.6) | Browser sign-in/out against the public API | DONE |
| 23 | Admin sign-in | api+admin | The console authenticates as an ordinary client of the same public API — no privileged back door (DR-11, AD-9). The scope is `api+admin`, corrected from `admin` in the task's unknowns batch: OQ-17 places the token handler ON the api (`/auth/admin/session`), which had no admin routes — the row could not meet its own deliverable without them. Session shape, cookie/CORS posture and the MFA slice are §12.5.6's task-23 rows (21 Aug 2026) | Admin sign-in/out e2e through the public surface, TOTP challenged on every sign-in | DONE |
| 24 | Social sign-in | api+web | Provider sign-in (FR-2, FR-4, D-6) behind `IdentityProviderPort` and its web flow. Three corrections from the task's unknowns batch (24 Aug 2026): **not passport middleware** — the redirect endpoints live on `apps/web` because only web writes the session cookie, so the api is a back channel with nothing for passport to mount on (§12.5.6's task-24 rows; `openid-client` pinned in §12.1, the OQ-48 revisit recorded); provider **behaviour** is config-store data while the client secret stays env until OpenBao (FR-82's rotation clause a recorded deferral); and **FR-8 link/unlink moved to task 27** — S-28 carries FR-7 and FR-8 on one screen, built once there. Interim recorded: a register-intent collision refuses with guidance (BR-ID-3) and a password account cannot yet link a provider | Provider flow e2e; an adapter swap changes no caller behaviour (P-7) | DONE |
| **25** | **Memberships and roles API** | api | Organization membership with the actor roles of [actors.md](actors.md) (CA, RC, OA). **Owns S-01's §4.3 post-sign-in branch** (none → S-04, one → S-05, several → organization choice), deferred from task 22 — until it lands, sign-in honours `?return=` and otherwise lands on `/home`, an assumption recorded rather than a decision | Role-gated membership endpoints with a role-matrix test; sign-in's exit branches on real memberships | DONE |
| 25.1 | Membership table and the role vocabulary | api | `identity.membership` with `ENABLE` **and** `FORCE ROW LEVEL SECURITY` and its policies in the same migration, `core.capture_field_change` attached so a role change is auditable from its first write (FR-55), and the role set declared once as an `as const` mirroring the `CHECK` constraint — the migration's own copy stays literal, per CLAUDE.md's closed-vocabulary exception. **The schema reads `identity`, corrected 25 Aug 2026 from this row's original `core`:** `architecture.md` §7.1 lists memberships under `identity` and names the one permitted cross-schema FK as "to `core.organization` only (membership target)", which the schema-invariant gate has encoded since task 9 — the document governs and the row was what was wrong | The table exists, the schema-invariant gate covers it, a cross-tenant read returns nothing | DONE |
| 25.2 | Membership use cases and the role matrix | api | UC-59, UC-62, UC-63, UC-64 (FR-56, FR-58, FR-59, FR-60) as use cases behind role-gated endpoints: list the organization's users, change a role with immediate effect, remove a member without erasing the audit trail, grant OA to avoid the single-admin lockout. Each `execute` takes one named Command | A role-matrix test per actor; removing the last OA is refused | DONE |
| 25.3 | Membership read for the session | api | UC-16's *view memberships* half (FR-12) — which organizations this account belongs to. Distinct from 25.2's OA-side view of one organization's users, and it is what task 28's `AuthGuard` resolves an active organization from. The *switch* half belongs to the global-tier switcher (`design_spec.md` OQ-6), which is 30.1 | The caller's memberships readable; the shape task 28 needs is fixed here rather than inside the guard | DONE |
| 25.4 | S-01's post-sign-in branch | api+web | §4.3's exit made real: none → S-04, one → S-05, several → the organization choice. Replaces the recorded interim in `apps/web/src/features/identity/constants.ts` and the `?return=`-or-`/home` landing in `social-flow.ts`, both of which name this task as their owner. **Scope reads `api+web`, widened from the parent's `api`:** the branch is web code over 25.3, the same correction task 23's batch made to its own row **Was blocked on 28.1, which was done first and closed the block (25 Aug 2026).** The branch reads the caller's memberships, and no authenticated route works until `AuthGuard` exists — nothing in `apps/api/src` resolves a bearer token into an actor, so `GET /api/v1/memberships` answers 401 to `apps/web` as to everyone. The two workarounds were both worse: building the guard under this number would falsify 28.1's row, and putting memberships on the sign-in response contradicts a decision recorded on `SessionResponseDto` in terms. **`?return=` precedence decided in the same batch:** it is honoured only when an active organization resolves, since returning a member-of-nothing to a deep link inside `(app)` lands them on a screen that cannot render **S-35 added to the inventory in this task's batch:** the project owner declined S-05's own error state for the case where the membership read fails, so a dedicated Focus screen says so instead — an amendment to `design_spec.md` §4.4 under UX-7, numbered 35 because S-29 … S-34 are the public tier | Sign-in branches on real memberships; both interim constants are deleted, not left dead | DONE |
| **26** | **Invitations** | api+web | Invitation issue, accept and expiry — API and screens together | Invitation flow e2e from the browser, including expiry | DONE |
| 26.1 | Invitation issue, resend and revoke | api | `identity.invitation` under 25.1's RLS pattern, single-use, expiry as data. UC-60 and UC-61 (FR-57), with the email dispatched through task 15's outbox and task 19's `EmailPort` — never inside the request transaction, which is the dual write P-8 removes. **The schema reads `identity`, corrected 25 Aug 2026 from this row's original `core`** — the same correction 25.1 made to its own row, and for the same reason: §7.1 lists invitations under `identity`, and the document governs. **Three unknowns closed in this task's batch** (§12.5.6's task-26.1 rows): a resend rotates the token and restarts the seven days on the same row; the email language is the invited address's account locale where one exists and the inviting administrator's otherwise; and both collisions — active member, live pending invitation — are refused with their resolving action named. **`GET /api/v1/invitations` lands here too**, widening the row's three verbs: FR-56 renders "active or pending invitation" as a union across two tables, and a resend/revoke API with no way to discover what is outstanding cannot close on its own, which is what a sub-step is sized to do. **Recorded deferral:** UC-60's seat-entitlement precondition and UX-50's quota path are not gated — `EntitlementPort` has no implementation until task 54 and `EntitlementGuard` does not exist, so an invitation beyond the plan's allowance is issued. What changes if it is wrong: one `@RequiresEntitlement('org.seats.max')` on the issue route. **A fourth unknown surfaced mid-build and is recorded rather than closed:** both write routes send mail to a third party and §12.5.6's auth-path throttle covers invitation *accept* only, so they are an accountable but unthrottled mail amplifier until task 71's edge limit — §12.5.6's task-26.1 amplification row carries what holds meanwhile | Issue, resend and revoke e2e; the invitation email leaves through the outbox | DONE |
| 26.2 | Invitation acceptance and expiry | api | UC-15 (FR-11): accepting creates the 25.1 membership at the invited role. The paths that are not the happy one are the work — expired, already consumed, already a member, and signed in as an account the invitation does not name. **Four unknowns closed in this task's batch** (§12.5.6's task-26.2 rows, 25 Aug 2026): the bearer read is a third transaction-local binding `app.current_invitation` with its own SELECT policy, because S-03 renders the inviting organization to a visitor who is still signed out; **registering from a live invitation for that same address creates an already-verified account**, which is task 24's provider-vouching precedent and amends FR-1 and FR-3; acceptance writes `identity.session.active_organization_id`, becoming its third writer; and an address already held by an **active** member consumes the invitation and leaves that member's role untouched. **Scope stays `api`** — the register route gains one optional field, which is where the verification decision lands; S-03 is 26.3's. **A fifth item was found mid-build and built rather than recorded:** §12.5.6's auth-path row names invitation *accept* among the throttled paths and this task had not implemented it — building it exposed a real defect, since the refusals threw inside the acceptance transaction and would have rolled back the very `auth_attempt` row the throttle rests on | Accept e2e including expiry; a consumed invitation cannot be replayed | DONE |
| 26.3 | S-03 accept invitation | web | The Focus screen in three locales, entered from the email link both signed out and signed in — the signed-out entry hands off to S-01 and returns, which is UX-38's `?return=` contract task 24 already threads through the provider flow. **Four unknowns closed in this task's batch** (25 Aug 2026): S-03's "Controls and actions" are **routes, not forms** — the row's reading wins over the literal one and `design_spec.md` S-03 and S-01 are amended to say so, with the registration route carrying the invitation so 26.2's verified-account path serves the flow it was built for; the permission state names both addresses and offers to sign out and return; provider sign-in **is** offered, its refusal being a designed state rather than a dead end; and the invitation rides the URL rather than a sealed cookie (§12.5.6's task-26.3 row). **A fifth was raised mid-build and amends task 25.4:** `?return=` is honoured when the destination can *render*, not only when an organization resolves — `/invitation/{token}` renders for a member of nothing and is the one deep link such a person must return to, so the hand-off was landing them on S-04 with the invitation lost. **Two 26.2 defects were found and fixed here:** the preview's per-IP throttle made every invitee behind one office NAT share a five-per-quarter-hour budget, and `invitationToken: null` answered 500 | A user joins an organization from the browser; expiry is a designed state, not an error page | DONE |
| 26.4 | S-16 users and access | web | The OA's Index: 25.2's role change and removal alongside 26.1's invite, resend and revoke, on one screen. **Ownership settled in this task's batch (26 Aug 2026): 26.4 owns the whole screen**, and this row names it — the correction the row itself invited, of the kind task 23's batch made. The argument is that S-16 *is* the union: task 25.1's migration records that "active or pending invitation" is one list assembled across two tables, so splitting the screen between tasks 25 and 26 would build that union twice and throw one away. Both API halves exist and are e2e-proven. **Three further decisions, two of them overriding the recommendation.** The seat-consumption region and UX-50's entitlement gate are **omitted**, deferred to 54.2 — three of UX-50's four required values are unknowable until the entitlement service exists. The Index archetype's fixed elements ship **in full, pagination included** (§4.6), even though `/members` and `/invitations` are unpaginated by design. And the **§4.2 workspace navigation tier is built here** rather than waiting for 30.1, as an inventory addition in `packages/ui` — which is what keeps UX-89 satisfied  **Two further unknowns closed in a second batch (26 Aug 2026), raised when the screen itself was started.** The **page-load rotation point moves into `proxy.ts`** — S-16 is the first Server Component to read the API on load, and neither the proxy (which checked only that the sealed cookie existed) nor `api-client.ts` (which may not write cookies during render) rotated the ≤15-minute access token, so a member returning after twenty minutes met a 401 with six days left on their session; `architecture.md` §12.5.6 carries the decision and `apps/web/CLAUDE.md` predicted it three tasks early. And **UC-175’s manual reminder is omitted**, its ownership written into task 50 — it is in S-16’s controls and FR list and was in no task at all. | S-16 live in three locales, every UC-59 … UC-64 action reachable | DONE |
| **27** | **Opt-in TOTP and password change** | api+web | TOTP enrolment, challenge on sign-in, recovery codes — API and screens together. **Plus FR-7, change own password** (architecture.md OQ-56, 21 Aug 2026): it needs an authenticated caller and a settings surface, and this is the security-settings slice. **Owns task 23's recorded TOTP debt** (§12.5.6's task-23 MFA row): encryption-at-rest for stored secrets — the admin realm's `totp_secret` included (`otpauth` is pinned since 24 Aug 2026, so the library question is closed) — plus the **segmented code-input as a §11.5 inventory addition**: the A-01 artboard draws it, the admin screen ships a plain field meanwhile, and the tenant challenge here is its second consumer. **Plus FR-8, link and unlink provider identities** (task 24's unknowns batch, 24 Aug 2026): S-28 carries FR-7 and FR-8 on one screen, so both halves land together here — a link established only after authentication by an existing credential, removal of the last credential refused (UC-11, UC-12); until then task 24's collision path refuses with guidance and a password account cannot link a provider | 2FA e2e: enrol, challenge, recover; password change with optional termination of other sessions; link/unlink e2e with the last-credential refusal | DONE |
| 27.1 | Secret encryption at rest | api | Task 23's recorded debt (§12.5.6's task-23 MFA row), paid **before** a second secret store exists: one encryption mechanism, applied to the admin realm's existing `identity.admin_account.totp_secret` by migration and inherited by 27.2. Doing it first is what stops the tenant table shipping plaintext and being migrated twice. **Three unknowns closed in this task's batch** (26 Aug 2026; §12.5.6's task-27.1 rows): the key is its **own** `SECRET_ENCRYPTION_KEY` rather than a third HKDF label on `AUTH_ADMIN_SECRET`, because a session secret rotates at no cost to data and an at-rest key does not; the envelope carries a **key version** with exactly one live key, so NFR-61's rotation is representable without shipping a second key nobody has scheduled; and "unrepresentable" is enforced as a **type** — `identity.encrypted_secret`, a domain whose constraint every writer meets, the api, the CLI and a `psql` prompt alike — with a schema-invariant classification so 27.2 inherits it rather than remembers it. **One correction found by running it:** the invariant's first draft proved the application role could still write the column by `SET ROLE esg_app`, which §7.6 forbids the migration owner — the role separation is real, so that half is proven where `esg_app` actually connects | Existing admin secrets re-encrypted in place; a plaintext secret is unrepresentable | DONE |
| 27.2 | TOTP enrolment and recovery codes | api | `otpauth` (§12.1, pinned 24 Aug 2026 — the library question is closed) behind enrolment: secret issue, first-code confirmation, recovery codes stored hashed and single-use. **Three unknowns closed in this task's batch** (26 Aug 2026): NFR-95 promoted opt-in tenant TOTP into MVP on 18 Aug 2026 and **nothing was written to carry it** — no use case, no MVP requirement row, no screen content — so `UC-193 … UC-195` are appended and `design_spec.md` S-28 and S-01 are amended in the same edit, S-28 gaining the management controls and S-01 the staged challenge; **enrolling and disenrolling both require the current password**, FR-8's link rule applied for its own reason, with a provider-only account's session standing as the credential as a recorded assumption; and recovery codes are **ten × sixteen Crockford base32 characters, SHA-256, single-use** — §12.5.6's ≥256-bit token row governs emailed links, not something a person retypes. **Scope widened by one item, stated rather than discovered:** disenrolment ships here, because opt-in that cannot be reversed is not opt-in (NFR-95) and 27.7's screen has to offer it | Enrol and consume a recovery code e2e | DONE |
| 27.3 | The TOTP challenge on tenant sign-in | api | The second step folded into task 21's sign-in without changing its unchallenged path — the same two-step shape `POST /auth/admin/session/challenge` already holds for the realm, so this follows a decided pattern rather than inventing one. **Two unknowns closed in this task's batch** (26 Aug 2026; §12.5.6's task-27.3 rows): the admin realm's challenge rides a cookie **the api itself sets** (OQ-17) and the tenant realm has no such cookie (OQ-33, AD-9), so the challenge is **sealed into the response body** instead — stateless, five minutes, not single-use, keyed by HKDF from `AUTH_JWT_SECRET` under its own label, with `apps/web` holding it as it holds task 24's OAuth transaction; and the flow stays **one route with a discriminated answer** rather than the admin's two calls, because splitting it would cost every user a round trip to serve the minority who opted in. **A gap this batch found and did not close here: no task built S-01's challenge screen** — 27.4 is the `packages/ui` input and 27.7 is S-28 alone — so `27.8` is appended for it | Sign-in challenges an enrolled account and does not challenge others | DONE |
| 27.4 | The segmented code input | pkg:ui | A §11.5 inventory addition with every applicable §8.1 state designed first (UX-8, UX-90). The A-01 artboard draws it and the admin screen ships a plain field meanwhile, so 27.3's tenant challenge is its **second** consumer and A-01 is retrofitted here. UX-89 is why it is not inlined in either screen; paste and password-manager autofill must work (UX-108). **No unknowns needed raising and the reason is worth stating:** UX-108 decides the one real design question — the control is **one input painted to look like six cells**, because paste and `one-time-code` autofill both target a single field, which the usual six-input build defeats. **`design_spec.md` §11.5 is amended in the same change**: One-time code is the first true addition to that enumeration rather than a variant of something in it, which is what UX-89 means by an addition to the inventory. The artboard's code-window countdown is a `hint` **slot** — its two consumers time different windows (30 s on A-01, five minutes on S-01) — and A-01's **recovery-code route still has nothing to point at**, since 27.2 built recovery codes for the tenant realm only | The component in `packages/ui` with its state set; A-01's plain field replaced | DONE |
| 27.5 | FR-7 change own password | api | UC-10, which needs an authenticated caller — the reason `architecture.md` OQ-56 placed it here rather than with task 21. Optional termination of other sessions reuses the invalidation password reset already exercises — **with one correction: it cannot reuse it wholesale.** FR-7 says *other* and FR-6 spares nothing, so a second store method excludes the session making the change; revoking it would sign the user out of the screen they just used. **One unknown closed in this task's batch** (27 Aug 2026; §12.5.6's re-authentication row): a route that asks for the current password behind a session is a password **oracle**, and 27.2's three password-gated TOTP routes shipped the day before with nothing in front of them — so all four now share one throttle key, keyed on the account, deliberately **not** wired to FR-4's lockout. **A fourth `revoked_reason`, `password_changed`, is added by migration** rather than borrowing `password_reset`: the column exists so a support answer can distinguish the causes, and a change is not a reset | Password change e2e, with and without terminating other sessions | DONE |
| 27.6 | FR-8 link and unlink provider identities | api | UC-11 and UC-12: a link established only after authentication by an existing credential, and removal of the **last** credential refused — the rule that makes unlink safe. Closes task 24's two recorded interims: the register-intent collision refusing with guidance (BR-ID-3), and a password account that cannot yet link a provider. **One unknown closed in this task's batch** (27 Aug 2026; §12.5.6's task-27.6 rows): link and unlink both require the current password, on 27.5's key — a link adds a *way in*, so a stolen session that could attach a provider would outlive the password change its owner reaches for. **Two corrections found while building.** The link's authorization half is task 24's public challenge route **reused unchanged** — it needs no actor — so only the redemption is new and authenticated; the §12.5.6 row was written before that was checked and corrected in the same change. And BR-ID-3's refusal is **permanent**, not interim: what task 24 recorded was its *dead-end guidance*, which now names linking. **A finding far larger than this task, fixed here:** fourteen `DomainError` message keys had no catalogue entry in any locale — six from task 24, five from task 23 — so those refusals shipped with a title and **no `detail`**, giving NFR-79's first part only. `message-keys.spec.ts` is the gate that would have caught it | Link and unlink e2e; the last-credential refusal proven; task 24's interims deleted | DONE |
| 27.7 | S-28 credentials and linked identities | web | The one Record screen carrying 27.5, 27.6 and 27.2's enrolment entry — S-28 holds FR-7 and FR-8 together, which is why task 24's batch moved FR-8 here rather than building the screen twice | S-28 live in three locales; expansion harness and axe scan pass | DONE |
| 27.8 | S-01's second-factor step | web | **Appended 26 Aug 2026** (task 27.3's batch), because the screen `design_spec.md` S-01 now specifies had no owner: 27.4 builds the segmented input in `packages/ui` and 27.7 is S-28, a Record screen for FR-7 and FR-8. UC-194 and UC-195 on the Focus archetype — the staged code field over 27.3's discriminated answer, the route to a recovery code, and a wrong code as a **recoverable** state that keeps the reader on the step. It is different work from 27.7's and not a widening of it: a different archetype, and an actor who holds no session yet. Consumes 27.4's input, so it follows it | The staged step live in three locales; an enrolled account is challenged and an unenrolled one is not; expansion harness and axe scan pass | DONE |
| **28** | **Authorization guard chain** | api | The guard chain across the whole API surface, permissions derived from actors.md; errors as problem+json with the NFR-90 correlation id and no internal identifiers in `title`/`detail`. `AuthGuard` must register **before** `TenantTransactionGuard` — APP_GUARD order follows registration order, and it is what puts the organization in the context that guard reads. Task 11's e2e fixture in `apps/api/test/` is then replaced by the real resolution. **Owns a task-23 deferral (24 Aug 2026):** admin sign-in attempts written to `audit.system_audit_log` — the A-01 artboard's LOGGED disclosure ships only once it is true, with this task's request-tier audit capture | Anonymous requests 401; a role-matrix test per actors.md; error bodies pass the user-facing-text rule | DONE |
| 28.1 | `AuthGuard` and the guard order | api | Registered **before** `TenantTransactionGuard` — APP_GUARD order follows registration order, and this guard is what puts the organization into the context that one reads. Task 11's e2e fixture in `apps/api/test/` is replaced by real resolution over 25.3. **This row must precede 25.4** (recorded 25 Aug 2026): §4.3's branch reads the caller's memberships and no authenticated route works until this exists, so 25.4 is `BLOCKED` on it. The document's order and the build order differ here, as they already do in the appended section | Anonymous requests 401; the fixture is deleted, not disabled | DONE |
| 28.2 | Permissions across the surface | api | Permissions derived from [actors.md](actors.md) and applied to every route rather than to the ones someone remembered. A matrix test per actor is what makes "across the whole API surface" a checkable claim | A role-matrix test per actor; no route without a stated permission | DONE |
| 28.3 | Error bodies conform | api | problem+json carrying the NFR-90 correlation id, with no `FR-`/`UC-` identifier, enum member, table name or provider string in `title` or `detail` (CLAUDE.md's user-facing-text rule). The correlation id is the one reference a person is meant to quote | Every error body passes the rule as a test, not as a review note | DONE |
| 28.4 | Request-tier audit capture | api | Task 23's deferral, recorded 24 Aug 2026: admin sign-in attempts written to `audit.system_audit_log`, whose `organization_id` task 14 made nullable for exactly these platform events. **A-01's LOGGED disclosure ships only once this is true** — until then the artboard states something the code does not do | Admin sign-in attempts appear in the system audit log; A-01's disclosure becomes accurate | DONE |

## Phase 3 — Reporting core (§15.4 #3)

| # | Name | Scope | Description | Expected result (deliverables) | Status |
| --- | --- | --- | --- | --- | --- |
| **29** | **Organizations and entities API** | api | Organization profile and reporting entities, tenant-scoped CRUD under task 12's RLS. Every new `core` table needs `ENABLE` **and** `FORCE ROW LEVEL SECURITY` plus its policies in the same migration — the schema-invariant gate fails the build otherwise | CRUD e2e as OA and RC; the isolation test extended to the new tables | DONE |
| 29.1 | Organization creation and profile | api | UC-49 and UC-50 (FR-13, FR-14, FR-15). Creating an organization makes the caller its OA through task 25's membership — which is what task 12's `WITH CHECK (true)` on the tenant root's `INSERT` was written for. Profile is legal form, name, address and contact details **Three decisions taken in this batch (28 Aug 2026, project owner) and written into `architecture.md` §7.2 and AD-4's artefact table before any code:** FR-14's `core.org_relationship` carries **two typed axes** — `kind` as a `CHECK`, `organization_type` as a configuration key, because only the second is NFR-9's; **legal form is a configuration vocabulary scoped by country**, taken against the recommendation on record, with the stated consequence that a country registering none cannot host an organization at MVP; and **IDNO stays 29.2's**, matching UC-49 and S-04's field lists. `GET /organizations/legal-forms` was added beyond the row's wording, because a create route that refuses a country leaves 30.2 no way to know which ones it accepts | Create-and-edit e2e as OA; the creator holds OA on the new organization | DONE |
| 29.2 | Entity identifiers | api | UC-51 (FR-16): **the IDNO and an optional LEI**, recorded **and validated** — the check digits are the work. **Corrected 28 Aug 2026:** this row read "LEI, DUNS, EU ID and PermID", naming three identifiers `architecture.md` OQ-18 removed from MVP on 18 Aug 2026 — two validators, not four, and the row contradicted its own next sentence. `task.md` owns no decisions; FR-16 and OQ-18 govern **Closed 28 Aug 2026 with one half of the deliverable open and recorded:** LEI is complete — ISO 17442 shape plus ISO 7064 MOD 97-10, its corpus built from published register values rather than strings the implementation produced. **The IDNO's check digit is not implemented, and could not be.** Government Decision 272/2002 fixes the structure and states that the thirteenth digit is a check digit without giving the algorithm; a candidate reproduced **2 of 12** real IDNOs, which is chance, and the whole family it belongs to is excluded because the twelve equations are inconsistent over GF(11). §7.2 carries the refutation and says what a further sample would need | Both identifiers' format and check digits proven by a fixture corpus, valid and invalid cases both; nothing accepts a DUNS, EU ID or PermID | DONE |
| 29.3 | Reporting entities | api | UC-52, UC-53, UC-55 (FR-17, FR-18, FR-20): create, edit master data without altering filed reports, archive while retaining history. **Three decisions taken 28 Aug 2026 (project owner):** `core.entity_snapshot` ships now with its writer arriving in 31.1 (§7.2 takes the snapshot at period open); NACE is a **configuration vocabulary** seeded from the state's own CAEM Rev.2, whose 996 codes match NACE Rev.2 exactly at every level; and **an RC reads while an OA writes**, since UC-19 has the Contributor completing B1 from values that pre-populate from this record. **FR-18's point-in-time clause is structural only** — the table, its immutability and its isolation are proven, and nothing writes a snapshot until a period can be opened. Every new `core` table carries `ENABLE` **and** `FORCE ROW LEVEL SECURITY` plus its policies in the same migration, or the schema-invariant gate fails the build | CRUD e2e as OA and RC; the isolation test extended to the new tables | DONE |
| 29.4 | Consolidation scope | api | UC-54 (FR-19): the individual or consolidated reporting boundary, which B1 reads and which decides what task 38's calculator aggregates over. **Two decisions taken 28 Aug 2026 (project owner):** a member is a **named subsidiary** — name, optional IDNO/LEI, country — rather than a pointer to another reporting entity, since a Moldovan SME's subsidiaries are generally not themselves on the platform; and the basis is **nullable with no default**, because VSME asks the question explicitly and a default would answer it on the undertaking's behalf. One rule is enforced at the record rather than deferred to task 40 — `consolidated` with an empty boundary is a structural contradiction, not a completeness question | The boundary set on an entity and read back, with its effect on B1 stated | DONE |
| **30** | **Organization and entity screens** | api+web+pkg:ui | The profile and entity screens per design_spec — the first workspace screens, so §4.2's global tier lands here and **replaces task 22's interim signed-in strip** | CRUD from the browser in three locales; the real global-tier chrome carries sign-out | DONE |
| 30.1 | The §4.2 global tier | api+web | The first real workspace chrome, and it **replaces task 22's interim `SessionStrip`** — `apps/web/src/app/[locale]/(app)/layout.tsx` names this task as its owner. Carries the account corner, sign-out, and the active-organization switcher, which is `design_spec.md` OQ-6's *switch* half of UC-16 (the *view* half is S-05). Inventory components only, per UX-89 **The workspace tier already exists (26 Aug 2026):** task 26.4 built §4.2's second tier as a `packages/ui` inventory addition, because S-16 was the first `(app)/(workspace)` screen and had no way to be reached. This row builds the **global** tier over it and still replaces the interim strip; it extends the workspace nav's link set rather than creating it. | The real global tier on every `(app)` screen; the interim strip deleted | DONE |
| 30.2 | S-04 create organization | web | The Focus screen 25.4's "none" branch lands on (UC-49) | An organization created from the browser in three locales | DONE |
| 30.3 | S-15 organization profile and identifiers | api+web | The Record screen over 29.1 and 29.2, with identifier validation surfaced in NFR-79's three-part shape rather than as a format string. **The LEI field carries its expansion in every locale and is never labelled with the bare abbreviation** — `design_spec.md` S-15 states the rule and the reason (28 Aug 2026, project owner): to a Moldovan reader *LEI* reads as the currency, so a bare label on a screen carrying fiscal data invites an amount where an identifier belongs. Help text says most Moldovan SMEs hold none, so an empty field reads as expected rather than as an omission. **Legal-form labels land here too:** 29.1 registered the forms as configuration codes and OQ-43 ships their wording with the release, so a form without its catalogue key renders the key — an internal identifier on a screen | Profile and identifiers editable in three locales; the LEI field labelled, not abbreviated; every registered legal form resolves to a label in all three; the cover contact held and distinct from the platform contact; the attribution names the acting person | DONE |
| **30.4** | **S-13 entities index and entity record** | api+web+pkg:ui | Index + Record over 29.3 and 29.4, with the archived state a designed state and both Index empty states present (§4.6). **Split into three sub-steps 29 Aug 2026** (project owner) and widened past `web`: the screen cannot classify an entity without a way to *offer* CAEM's 996 codes, and there was no read route, no English labels and no Combobox | Entity CRUD from the browser; expansion harness passes | DONE |
| 30.4.1 | The activity classifier, offered rather than only validated | api+config+pkg:ui | FR-17's NACE code(s) need a picker and three things were missing at once. **A searchable read route** — `GET /entities/nace-codes` over the config store, answering code + label in the request's negotiated locale, bounded to a page of matches: `/organizations/legal-forms` ships ten keys whole, this is 996 and 182 KB, so server-side search is what keeps the wording where OQ-43 puts it and out of the bundle (NFR-43). **English labels**, which the seed lacked — task 29.3 sourced it from the Bureau's `caem_rev2.zip`, typeset in Romanian and Russian only. **`Combobox`**, a §11.5 Form controls entry that was never built; a 996-entry `Select` is not a control | The classifier searchable in three locales; the seed carries all three; `Combobox` in the inventory with its §8.1 states | DONE |
| 30.4.2 | S-13 the entities index | web | The Index archetype over `GET /entities` — both empty states (§4.6: first use teaches and offers creation; filtered distinguishes *nothing matches* from *nothing exists*), the archived state designed rather than defaulted, and the row action into the record. **Four of the artboard's six columns belong to other tasks** and are refused with their owners named: entity IDNO and its *verified* marker are FR-107's fiscal lookup on the billing account, employee count is B1 disclosure data (UC-19), the periods column is task 31's, and the entitlement counter is 54.2's — the same deferral S-16 recorded for seats | The list live in three locales with both empty states and the archived state; expansion harness passes | DONE |
| 30.4.3 | S-13 the entity record | web | The Record archetype over 29.3 and 29.4: name, legal form, activity codes on 30.4.1's Combobox, **sites** and **consolidation members** as editable sub-collections (both are whole-collection saves — the API's own semantics: a member with an id is edited, one without is added, an omitted stored member is removed), the consolidation basis FR-19 requires, and archiving as a consequence-disclosing action (§6.14, UX-70) naming what survives. **The record artboard omits consolidation entirely** and §5's Content row requires it; it also draws a single activity where FR-17 and the API carry a list | Entity CRUD from the browser in three locales; archive states its consequence before it happens | DONE |
| 30.5 | S-05 home and organization overview | web | UC-16's *view memberships* half, which 25.4's "one" and "several" branches land on. **UC-67's report-status region (FR-23) is a stated stub here** and is completed by 32.4 — there are no reports to summarize until task 31 exists, and drawing an explicitly empty region is honest where inventing data is not **Owes S-03's arrival sentence (recorded 26 Aug 2026, review):** `POST /invitations/acceptance` answers a `grant` of `created` / `reactivated` / `already_member`, and task 26.3's exit redirects here without stating which — so someone who already had access sees the same landing as a new member. The home states it; the vocabulary is `MEMBERSHIP_GRANT_KIND` in `@easyesg/contracts`. | S-05 live with its membership list; the report-status region present and explicitly empty; S-35's wording not duplicated in S-05's own error state; the arrival sentence stated | DONE |
| **31** | **Periods and report lifecycle API** | api | Reporting periods as calendar date + timezone (FR-21, NFR-34); report lifecycle and snapshots; template + taxonomy version pinned at creation (DR-4) | Period and report CRUD; the pinned version never moves silently | DONE |
| 31.1 | Reporting periods | api | UC-56 (FR-21, FR-45, FR-66): the period shell as **calendar date plus originating timezone**, never an instant (NFR-34). A period boundary is exactly the legal date an epoch value gets wrong | Periods created with their timezone; a boundary survives a timezone change | DONE |
| 31.2 | Period lock and reopen | api | UC-57 and UC-58 (FR-22): locking makes a distributed report stable and read-only; reopening is a *recorded* post-publication correction, so the record is the feature rather than a side effect | Lock, refuse a write, reopen with the reason captured | DONE |
| 31.3 | Report lifecycle and version pinning | api | DR-4: the template and taxonomy version are pinned **at creation** and never move silently. The pin is the invariant this slice exists to prove, not a column it happens to write | A report carries its pins; a test proves nothing but an explicit migration moves them | DONE |
| 31.4 | Report snapshots | api | The snapshot a locked period and an export both read, so the two cannot disagree about what was filed | A locked report reads identically before and after; a reopen is visible in the record | DONE |
| **32** | **Period and report screens** | web | Period setup and the report list/creation screens | A report created from the browser carries its pinned versions | TODO |
| **32.1** | **S-14 reporting periods** | **web+pkg:ui** | Index + Record over 31.1 and 31.2, with lock and reopen as designed states rather than confirmations bolted on | Periods managed from the browser in three locales | DONE |
| 32.1.1 | S-14's three inventory additions | pkg:ui | **Scope differs from the parent's, and why**: S-14 cannot be built out of what `packages/ui` holds. Three §11.5 rows have no implementation — **Date**, the **version pin indicator**, which S-14's own content list names and which is the first entry in an empty `domain/` folder, and the **reporting-period picker**, which §11.5 reserves as its own component *because of this screen*: *"reporting periods are the one place where a wrong date is expensive and invisible"*. Task 30.4.1 is the precedent — the Combobox before the screen that needed it. `Date` is a native `<input type="date">` in `TextField`'s clothes, since the value reads back ISO `YYYY-MM-DD` whatever the locale, which is what `LegalDate.date` stores (`architecture.md` §12.5.6) | All three in the inventory with their applicable §8.1 states stated and a spec that fails when the guarantee breaks; the picker owning the range rule so no screen restates it; no screen inlines any of them. **Three things the root file's UX-89 list names are deliberately not here**, each with its owner: the components artboard, which `CodeField`, `ProviderButton` and `AccountMenu` were all added without touching, so the practice is that it is a rendered reference (OQ-10) rather than a file each addition edits; **expansion coverage**, which is a Playwright harness over a screen and belongs to 32.1.2; and a **dark map**, because `tokens.css` defines no dark colour scheme at all | DONE |
| 32.1.2 | S-14 the Index and Record | web | The Index over `GET /periods?reportingEntityId=`, the Record over `GET/PATCH /periods/{id}`, and lock and reopen as **designed states rather than confirmations bolted on** — both are irreversible-class under UX-71, so each states its compensating mechanism, and a reopening is displayed on the period thereafter with its stated reason (UX-72). Content per §5: fiscal year, the three dates, the pinned versions on 32.1.1's indicator, the linked preceding period, and the lock state. **The timezone is the browser's resolved zone and no control collects it** — decided 31 Aug 2026 with its cost named on `architecture.md` §12.5.6 | Periods opened, edited, locked and reopened from the browser in three locales; both Index empty states present; the locked state read-only and saying how to get through it | DONE |
| **32.2** | **S-06 reports index** | **api+web** | UC-17 (FR-25): which reports are open to this user and their status, including the Index archetype's teaching empty state and its distinct filtered-to-nothing state (§4.6). **Widened past `web` on 31 Aug 2026** (project owner, `architecture.md` §12.5.6): `GET /reports` answered `reportingPeriodId` and nothing else, so the row that exists to name an entity and a year could only show an identifier — task 30.4.2's `codes=` decision with *report* substituted for *entity*. The **completion and validation columns §5 lists are refused**, their owners named: validation findings are task 40's and the completion roll-up 41.3's | The index live in three locales with both empty states, each row naming its entity and year; the two summary columns absent rather than drawn empty, with their owners recorded | BLOCKED |
| 32.2.1 | `GET /reports` answers its subject | api | **Scope differs from the parent's, and why**: the route answered `reportingPeriodId` and nothing else, so the row that exists to name an entity and a year could only show an identifier. Task 30.4.2's `codes=` decision with *report* substituted for *entity* (`architecture.md` §12.5.6). The projection is resolved by join on every read, list and record alike, so a creation flow and the record it lands on cannot read different shapes | Each row carrying its entity's name and its period's year and dates; one shape on the list and the record; a test that fails when the join is removed | DONE |
| 32.2.2 | S-06 the reports index | web | The Index over 32.2.1's read, with both §4.6 empty states. **The completion and validation columns §5 lists are refused**, their owners named: validation findings are task 40's and the completion roll-up 41.3's. **BLOCKED on S-07 (task 36)** — §4.4 has no report record screen, so S-06's only exit is the wizard, and `/reports/[reportId]/page.tsx` is a redirector task 36 owns. Shipping the Index against it is the dead row action task 30.1 ruled against and 30.4.2 was restructured to avoid; the scheduling call is the project owner's, like 33.1's pull-ahead | The index live in three locales with both empty states, each row naming its entity and year | BLOCKED |
| 32.3 | Report creation and its visible pins | web | The creation flow over 31.3, with the pinned template and taxonomy version **shown rather than implied** — DR-4 is only checkable by a user if the pin is on the screen | A report created from the browser carries and displays its pinned versions | TODO |
| 32.4 | S-05's report status overview | web | UC-67 (FR-23), completing 30.5's stated stub: whether everything is ready before the deadline, which is the question S-05 exists to answer | The overview answers the readiness question; 30.5's stub is gone | TODO |
| **33** | **Taxonomy registry** | api+config | Versioned VSME taxonomy read from the config store (AD-3, AD-4); labels resolved as catalogue keys (OQ-43), EFRAG translations where published (NFR-24); two taxonomy versions registered in staging from day one, a report pinned to each (R-7) | Taxonomy loads as data; the two-version staging setup exercises DR-4 continuously | DONE |
| 33.1 | The taxonomy artefact in the config store | api+config | **Pulled ahead of tasks 31.1 and 31.3** (project owner, 29 Aug 2026): both pin a taxonomy version and none existed. A versioned VSME taxonomy as **rows in task 16's generic store** (AD-3, AD-4) — no table and no code per artefact, which is what the store being generic is for | A taxonomy version registered as data; adding an element needs no code change | DONE |
| 33.2 | Label resolution | api | Element keys resolve through `platform/localization` from committed catalogues (`architecture.md` OQ-43 — behaviour is data, wording ships in the release), using the official EFRAG translation where one is published (NFR-24). **Romanian *and* Russian VSME labels are platform-authored with no EFRAG standing** — T-14 as amended 31 Aug 2026, when opening the `2026-05-01` package showed `ro`'s label linkbase is a stub — which the surface must state rather than imply | A label resolves in three locales, each carrying its EFRAG standing inseparably. **The reader-facing half of this row is refused and its owners named** (`architecture.md` §12.5.6, 1 Sep 2026): stating the platform-authored status *where a reader sees it* is UX-47's export dialogue, task **47.3**, and UX-98's exported document, task **46.3** — an `api`-scoped task puts nothing in front of a reader | DONE |
| 33.3 | The two-version staging setup | api+config | R-7: two taxonomy versions registered in staging **from day one**, a report pinned to each. This is the arrangement that exercises DR-4 continuously instead of at the first migration, which is when a version-pinning defect is cheapest to find | Two versions live in staging, a report pinned to each, both rendering | DONE |
| **34** | **Disclosure store** | api | The generic disclosure store and its typed facade (T-3). Attach `core.capture_field_change` to the value table — the comparison is over `jsonb` row images, so it needs no new plpgsql, only the trigger and a line in the invariant's audited list | Disclosure write/read round-trip through the facade | DONE |
| 34.1 | The generic value store | api | The value table under RLS — **typed columns, not `jsonb`**: §7.3's DDL governs and this row's wording was loose (`task.md` owns no decisions), so `value_numeric` stays `numeric` and is summable in SQL, which FR-45's comparatives and 34.3's prior-period read both need, with `core.capture_field_change` attached and a line added to the invariant's audited list — the comparison is over row images, so it needs no new plpgsql. Task 14's entry already names this table: an `UPDATE` written here that bypassed the trigger is the application-discipline failure the trigger exists to prevent | Values written and read under RLS; every field change audited without a call site asking for it | DONE |
| 34.2 | The typed facade | api | T-3's buy-back — the generic store gives up compile-time typing and per-disclosure check constraints, and the facade is what returns them. Generated from 33.1's taxonomy, so a new element produces a new typed accessor with no DDL change (NFR-86) | Write/read round-trip through the facade; a taxonomy addition regenerates it | DONE |
| 34.3 | The prior-period read path | api | FR-45 and FR-46's server half: last year's value for this year's field, which UC-45 displays and UC-46 carries forward. It belongs with the store rather than with the wizard because "the prior period" is a query across two differently-pinned reports, not a UI affordance | A prior-period value retrievable across two periods with different pins | DONE |
| **35** | **Wizard shell and autosave** | web | The B1–B11 wizard structure, navigation and autosave against its design_spec screens | Wizard navigable end-to-end; autosave survives reload | DONE |
| 35.1 | S-07's wizard shell | web+pkg:ui | The Wizard archetype: module list, step navigation, and the step **in the URL** — UX-4 requires every addressable state to be addressable, and a wizard whose step lives in React state fails it | B1–B11 navigable; every step has a URL that restores it | DONE |
| 35.2 | Autosave | web+pkg:ui+pkg:contracts | UC-35 (FR-37, FR-38) — the draft-integrity pattern UX-34 … UX-39 with its save-state indicator. UX-7 discharges UC-35 through this *pattern* rather than through a screen, so the pattern is the deliverable | Every field change persists with no explicit save; the indicator tells the truth about pending writes | DONE |
| 35.3 | Resume a draft | api+web | UC-36 (FR-39): the previous state on any device with no data loss — the same pattern's other half, and the half a reload actually exercises | A draft survives reload and a second device | DONE |
| **36** | **Wizard disclosure forms** | web | The B1–B11 forms bound to the disclosure store, labels from catalogue keys, three locales | A user completes B1–B11 and the store holds it; expansion harness passes | TODO |
| 36.1 | The disclosure field anatomy | pkg:ui | The §11.5 domain components every module below reuses, with all applicable §8.1 states designed before the first instance (UX-8, UX-90). UX-89 bites hardest here: eleven modules copying a bespoke field would copy eleven omissions — no state set, no dark map, no expansion coverage, no accessibility review | The field components in `packages/ui` with their state sets and dark maps | DONE |
| 36.2 | B1 — basis for preparation | web | UC-19 (FR-24, FR-27, FR-28) — **First, because it drives applicability.** B1's answers decide which later modules are in scope, so building any of them before it means building against a guess — and it is what gates every conditional section under UX-9 | B1 complete and stored; the applicability it drives is observable on the module list | TODO |
| 36.3 | B2 — practices, policies and future initiatives | web | UC-20 (FR-24) — Narrative-dominant, and the first module where UX-95's named-value rule meets long-form text rather than figures | B2 complete and stored in three locales | TODO |
| 36.4 | B3 — energy and GHG emissions | web | UC-21 (FR-24, FR-29) — Carries the seam 39.2 writes calculated emissions into, so its fields must accommodate a value arriving from elsewhere and say where it came from | B3 complete; calculated fields present and marked as such | TODO |
| 36.5 | B4 — pollution | web | UC-22 (FR-24, FR-29) — Emissions to air, water and soil, **or an explicit exclusion** — 36.13's terminal state is the common answer here, not an edge case | B4 complete, including the reasoned-exclusion path | TODO |
| 36.6 | B5 — biodiversity | web | UC-23 (FR-24, FR-28) — **Site-driven conditional applicability** (§6.1 row 2): proximity to a sensitive area decides whether the section appears at all | B5 complete; the site-driven condition proven both ways | TODO |
| 36.7 | B6 — water | web | UC-24 (FR-24, FR-28, FR-29) — **Sector-driven conditional applicability** (§6.1 row 2) — withdrawal, high-stress share and consumption | B6 complete; the sector condition proven both ways | TODO |
| 36.8 | B7 — resource use, circular economy and waste | web | UC-25 (FR-24, FR-29) — Circularity practices alongside waste quantities — narrative and figures in one module, which 36.1's anatomy has to carry without a bespoke field | B7 complete and stored | TODO |
| 36.9 | B8 — workforce characteristics | web | UC-26 (FR-24, FR-28, FR-29) — Headcount and FTE by contract type, gender and country. The most table-shaped module in Basic, and the one C5 extends (79.5) | B8 complete; the breakdowns entered as a table, not eleven fields | TODO |
| 36.10 | B9 — health and safety | web | UC-27 (FR-24, FR-30) — Recordable accidents, accident rate and fatalities — a computed rate beside its inputs, so the derivation is visible rather than asserted | B9 complete; the rate derived and traceable to its inputs | TODO |
| 36.11 | B10 — remuneration, collective bargaining and training | web | UC-28 (FR-24, FR-28, FR-29) — **Threshold-driven conditional applicability**: ≥150 employees brings the gender pay gap into scope, ≥50 the turnover rate (§6.1 row 2) | B10 complete; both thresholds proven at and either side of the boundary | TODO |
| 36.12 | B11 — corruption and bribery | web | UC-29 (FR-24, FR-30) — Convictions and fines, where **absence is a positive statement** rather than an empty field — the distinction 36.13 exists to keep | B11 complete; a nil return is distinguishable from an unanswered one | TODO |
| 36.13 | Not material, and not available | web | UC-30 and UC-31 (FR-31, FR-32): a reasoned section exclusion, and an explained gap as a **valid terminal field state** rather than an error. Every module above needs both, which is why this is one slice and not eleven | A section excluded with a reason satisfies validation; an explained gap is terminal | TODO |
| 36.14 | Prior-period values and carry-forward | web | UC-45 and UC-46 (FR-46, FR-47) over 34.3: last year's value beside this year's input, and one action to carry an unchanged one forward | Prior-period values visible in the wizard; carry-forward writes through the store | TODO |

## Phase 4 — Calculator and validation (§15.4 #4)

| # | Name | Scope | Description | Expected result (deliverables) | Status |
| --- | --- | --- | --- | --- | --- |
| **37** | **Factor sets as configuration** | api+config | Emission factor sets as effective-dated config (AD-4) | A factor change lands with no redeploy | TODO |
| 37.1 | The factor-set artefact and its schedule | api+config | Emission factor sets as effective-dated rows in task 16's store, using §7.9's `WITHOUT OVERLAPS` key so two sets cannot both be in force on one date — the constraint, not a convention | Factor sets registered as data; an overlapping effective range is refused by the database | TODO |
| 37.2 | Factor resolution at a date | api | The read path task 38 pins a run to: which set was in force on the calculation's date, asked as a **calendar-date** question (NFR-34) rather than an instant one | A factor resolved for a date; a factor change lands with no redeploy and reverts in one step | TODO |
| **38** | **Calculation runs** | api | Scope 1 + location-based Scope 2 calc runs with retained inputs (P-11), results feeding B3 | A calc run reproducible from its retained inputs | TODO |
| 38.1 | The run record and its retained inputs | api | P-11: inputs retained **at the run**, not recomputed later, under RLS and with 37.2's factor-set version pinned. This is the table reproducibility rests on, and retrofitting it is what P-11 exists to prevent | A run records every input and the factor version it used | TODO |
| 38.2 | Scope 1 | api | The direct-emissions calculation as domain code — no NestJS, no TypeORM, no HTTP (the dependency rule), so its test runs with no database and no broker | Scope 1 computed and unit-tested with no infrastructure | TODO |
| 38.3 | Location-based Scope 2 | api | FR-34, FR-35: purchased energy at the location-based grid factor, kept separate from 38.2 because the factor source and its effective dating differ | Scope 2 computed and unit-tested with no infrastructure | TODO |
| 38.4 | Results into B3, and reproducibility | api | The write into task 34's store, and the proof that matters: re-running from 38.1's retained inputs yields the same number **after the factor set has moved on** | A run reproducible from its retained inputs across a factor change | TODO |
| 38.5 | Override and annotation | api | UC-34 (FR-36): accept, explain or replace a computed figure. An override is a recorded disclosure, so it carries its reason and its audit row rather than overwriting silently | An overridden figure keeps both values and the reason | TODO |
| **39** | **Calculator screens** | web | Activity-data entry and results flowing into B3 inside the wizard | The calculator usable from the browser; B3 receives the result | TODO |
| 39.1 | S-09 activity-data entry | web | UC-32 (FR-33): consumption by source and site **in invoice units**, because that is what the user is reading off a bill — conversion is the system's job, not theirs | Activity data entered in three locales in the units the source document uses | TODO |
| 39.2 | Results and the return into B3 | web | UC-33: the Wizard sub-flow computing and handing back, landing in the field 36.4 marked as calculated | The calculator run from the browser; B3 receives the result | TODO |
| 39.3 | Review, annotate or override | web | UC-34 over 38.5, with the override reason a required field **carrying a message** — `@easyesg/ui/forms`' `BoundRules` narrows `required: true` out of the type precisely because a message-less rule renders no inline text, no `aria-invalid` and no summary entry | A figure accepted, explained or replaced from the browser | TODO |
| **40** | **Validation rule interpreter** | pkg:validation | Rule definitions as data, one interpreter shared by api and web (§9.8) so the server verdict and the inline verdict cannot drift | The same rule set evaluated identically in both runtimes, proven by a shared fixture corpus | TODO |
| 40.1 | The rule definition | pkg:validation | What a rule *is* as data (§9.8) — the schema 41.1 stores and A-05 edits. Wrong in either direction is expensive: too narrow and rules need code; too loose and the interpreter becomes a language | A rule set expressible as data with no code per rule | TODO |
| 40.2 | The interpreter | pkg:validation | Pure evaluation, no database and no HTTP, so both runtimes can hold the same one. This is the single artefact that makes drift between the server verdict and the inline verdict impossible rather than merely unlikely | Rules evaluated in a test with no infrastructure | TODO |
| 40.3 | The shared fixture corpus | pkg:validation | The proof: one corpus run in `apps/api`'s runtime and in `apps/web`'s, asserting identical verdicts. Without it, "shared interpreter" is a claim about imports rather than about behaviour | The same corpus green in both runtimes, in one gate | TODO |
| **41** | **Server-side validation** | api | The interpreter wired into the API: rule sets from the config store, findings persisted per report | Server verdicts recorded; a rule change lands as data | TODO |
| 41.1 | Rule sets from the config store | api | 40.1's definitions as effective-dated rows (AD-4), so a rule change lands as data and reverts in one step | A rule change takes effect with no redeploy | TODO |
| 41.2 | Findings persisted per report | api | The findings table under RLS, and UC-40 (FR-43): re-evaluating at any level of completeness, which means a re-run **replaces** a report's findings rather than accumulating them | Findings recorded and re-runnable; a corrected field clears its finding | TODO |
| 41.3 | Section and report completion status | api | UC-38 (FR-41): the single readiness signal before export, computed from 41.2 server-side rather than counted in the browser — two counters would be two answers | One status per section and per report, server-computed | TODO |
| **42** | **Inline validation and roll-up** | web | Inline states in the wizard and the report-level roll-up; findings in the NFR-79 shape (what happened / so what / what now), no internal identifiers or enum members in the text | Wizard shows finding states live; roll-up gates submission per the rule definitions | TODO |
| 42.1 | Inline field state | web | UC-37 (FR-40): each field's state at the point of entry, over 36.1's anatomy. **`design_spec.md` OQ-49 — how a verdict reaches a field — is open with its assumption recorded, and this slice is where it closes.** Closing it in passing while building something else is what the open-question rule forbids | Field states live in the wizard; OQ-49 written into the artefact that owns it before the code | TODO |
| 42.2 | S-08 the validation panel | web | The persistent in-wizard Panel over 41.3's roll-up (UC-38), gating submission **per the rule definitions** rather than per a hardcoded list | The panel live and persistent; submission gated by the rule set | TODO |
| 42.3 | Finding to field | web | UC-39 (FR-42): reaching the field that produced a finding directly — a URL, per UX-4, not a scroll handler | Every finding navigates to its field, addressably | TODO |
| 42.4 | Finding text conforms | web | NFR-79's three parts — what happened / so what / what now — with no rule id, enum member or taxonomy element key in what the reader sees. The reader is an SME owner or a bookkeeper, and `VALUE_INCONSISTENCY` occupies exactly the space the "what now" belongs in | Every finding renders in the three-part shape; the rule is a test, not a review note | TODO |

## Phase 5 — Export (§15.4 #5)

| # | Name | Scope | Description | Expected result (deliverables) | Status |
| --- | --- | --- | --- | --- | --- |
| **43** | **Report preview** | api+web | HTML preview from the same rendering the PDF will use, so the two cannot drift (FR-48, FR-49) | Preview renders a full report in three locales | TODO |
| 43.1 | One rendering, two consumers | api+web | The renderer both the preview and task 44's PDF read, built once — FR-48 and FR-49 cannot drift only if there is nothing to drift between. Deciding this before the PDF pipeline exists is the point; unifying two renderers after both exist is the expensive order | One rendering module, with the preview as its first consumer | TODO |
| 43.2 | S-10 report preview | web | UC-41 on the Document archetype: the assembled report without generating a file, in three locales | A full report previewed in three locales | TODO |
| **44** | **PDF pipeline** | worker | Playwright/Chromium PDF on the worker via queue (DR-10, AD-10), registering for the job **name** on task 15's single queue — **the first *long-running* consumer, not the first consumer**: task 19 put verification email on that queue, because sending inside the request transaction is the dual write P-8 removes (corrected 20 Aug 2026); whether export needs a queue of its own (NFR-46's wording) is a capacity question to settle here, with something to measure: container-isolated, memory-capped, per-job timeout, retry on a fresh instance (T-13) | A queued export yields a tagged PDF; a hung render is killed and retried; nothing renders in the request tier | TODO |
| 44.1 | The `renderer` image | infra | Deferred here from task 18 with the pipeline it serves: Chromium installed **explicitly** (`pnpm exec playwright install --with-deps chromium`, never left to `postinstall`), container-isolated with a hard memory limit and a restart policy (T-13) | The image builds in CI and runs; Chromium present by construction, not by luck | TODO |
| 44.2 | The export consumer | worker | Registering for the job **name** on task 15's single queue. Task 19 already put verification email there, so this is the first *long-running* consumer and not the first consumer — the correction recorded 20 Aug 2026. Nothing renders in the request tier (DR-10, AD-10) | A queued export produces a PDF; the request tier never renders | TODO |
| 44.3 | Tagged PDF output | worker | FR-44 and FR-49: a publication-ready **tagged** PDF — the version-sensitive half of T-13, and the half a Chromium bump can silently regress | A tagged PDF validated by veraPDF in the Compose stack | TODO |
| 44.4 | Hang, kill, retry | worker | T-13's operational half: a per-job timeout, a killed render, a retry on a fresh instance. The deliverable is the **hung** case — the healthy one proves nothing about the component's known failure mode | A deliberately hung render is killed and retried; the job still completes | TODO |
| 44.5 | The queue-of-its-own question | worker | NFR-46's wording leaves open whether export needs a queue separate from AD-10's single one. **A capacity question settled here with something real to measure**, rather than assumed in either direction back at task 15 | Measured, decided, and the decision written into `architecture.md` — not left as a source comment | TODO |
| **45** | **EFRAG template patching package** | pkg:xlsx-patch | Patch the official EFRAG Digital Template rather than regenerate it | The package round-trips the template with values patched in | TODO |
| 45.1 | Round-trip fidelity | pkg:xlsx-patch | Open the official template and re-emit it unchanged. This comes first because it is the property the whole approach rests on: patching rather than regenerating is only safe if a no-op patch is provably a no-op | The template round-trips unchanged, or every difference is enumerated and explained | TODO |
| 45.2 | Value patching | pkg:xlsx-patch | Values written into the template's own cells, with formulas, formatting and data validation left alone | Values patched in; the workbook still opens and still calculates | TODO |
| 45.3 | `config/efrag/` — the template registry | config | Which template version, pinned, alongside the taxonomy version a report already pins (DR-4) | A report's export resolves the template version its pin names | TODO |
| **46** | **Excel export integration** | worker | The xlsx-patch package on the export path; LibreOffice Calc headless round-trip as the automated gate, desktop Excel a release-checklist item (T-12); the platform-authored status of the Romanian and Russian labels stated in the export (T-14 as amended) | Export passes the Calc gate; the golden-report cross-format corpus started | TODO |
| 46.1 | The Excel consumer | worker | 45.2 on the export path, beside 44.2 on the same queue and behind the same job-name routing | A queued Excel export yields a populated template | TODO |
| 46.2 | The LibreOffice Calc gate | worker | Headless Calc round-trip as the **automated** gate; desktop Excel stays a release-checklist item, because server-side Office automation is unsupported on Linux (T-12). Naming what is not covered is part of the deliverable, not a caveat on it | The Calc gate runs in CI; the Excel checklist item exists in the release runbook | TODO |
| 46.3 | The platform-authored-labels statement | worker | T-14 as amended 31 Aug 2026: **Romanian and Russian** VSME labels are platform-authored with no EFRAG standing — `ro`'s linkbase in the `2026-05-01` package is a stub — and the export **says so** rather than implying parity with the one locale that is official — a bank or EU buyer reads the file, not this table | The statement present in the Russian export, in the export itself | TODO |
| 46.4 | The golden-report corpus | worker | Started here, across both formats: one report rendered to PDF and to Excel, diffed against a stored expectation. NFR-19's nightly replay reads it later, so its shape is decided now | A corpus of at least one full report, cross-format, replayable | TODO |
| **47** | **Export history** | api+web | Export records carrying their pinned template + taxonomy version (DR-4), artifact storage, re-download — API and screens together | Export history per report; artifacts retrievable; the pinned version visible | TODO |
| 47.1 | Export records and artifact storage | api | Each export carrying the template and taxonomy version it was produced at (DR-4), with the artifact **stored** rather than regenerated — UC-44 asks for a prior export *exactly as distributed*, which a regeneration cannot promise once anything upstream has moved | An export retrievable byte-identical months later | TODO |
| 47.2 | Export language | api | UC-48 (FR-52): export language chosen independently of interface language. A Romanian-reading user producing an English report for a bank is the case this exists for | Export language selectable and independent of the session locale | TODO |
| 47.3 | S-11 export dialogue and history | web | UC-42 … UC-44 (FR-53) on Panel + Index: request an export, watch it complete, re-download an old one with its pinned version visible | Export history per report in three locales; artifacts retrievable | TODO |
| **48** | ****Free-tier pilot milestone**** | — | The gate §15.4 places here: pilot SMEs produce a real VSME report end-to-end on the free tier, so real feedback arrives before the billing stack is finished (R-8) — timed against the April–May filing window | A pilot organization registers, reports B1–B11, and exports PDF + EFRAG Excel with no operator intervention | TODO |
| 48.1 | The preconditions confirmed | — | **Task 48 is a gate, not a build task** — it has no deliverables of its own, so its sub-steps are checks. **Task 75's legal slice must have landed**, which is §15.4's third scheduling fact rather than this file's opinion: GDPR Article 13 and Law No. 195/2024 (applicable 23 August 2026, NFR-5) bind where personal data is collected, and this is where real SMEs arrive. Checked before a pilot organization is invited, not after | The legal documents live, `SiteFooter`'s links resolve, and the consent decision is recorded | TODO |
| 48.2 | The end-to-end rehearsal | — | A pilot organization registers, reports B1–B11 and exports PDF and EFRAG Excel **with no operator intervention**. The phrase is the acceptance criterion: any step that needed a hand is a finding, and finding them is what the milestone is scheduled before the billing stack for (R-8) | The journey completed by someone who is not on the team, and everything they hit recorded | TODO |

## Phase 6 — Notifications (§15.4 #6)

| # | Name | Scope | Description | Expected result (deliverables) | Status |
| --- | --- | --- | --- | --- | --- |
| **49** | **Notification core** | api | Categories with behaviour in the config store (AD-4); task 19's minimal mail port absorbed, not duplicated | Category-driven dispatch; behaviour changes without redeploy | TODO |
| 49.1 | Categories as configuration | api+config | Category **behaviour** — channel, cadence, whether opt-out is permitted — as rows in task 16's store (AD-4). **Wording is not here**: `architecture.md` OQ-43 narrowed the store to behaviour, and templates ship as committed catalogues | A category's behaviour changes with no redeploy | TODO |
| 49.2 | Task 19's mail port absorbed | api | The minimal `EmailPort` built at task 19 moves behind this module rather than being duplicated beside it (P-7). Task 19's row said the notification system "must not wait for it"; this is the other half of that sentence, and skipping it leaves two mail paths | One mail port in the codebase, not two; task 19's callers unchanged | TODO |
| 49.3 | Category-driven dispatch | api | Every notification raised as a category and routed through the outbox onto task 15's queue — the path verification email already takes, so nothing new is invented for the general case | A notification dispatched by category through the outbox | TODO |
| **50** | **In-app centre** | api+web | The in-app notification centre — endpoints and screens together. **It also owns UC-175 and FR-173’s manual reminder** (recorded 26 Aug 2026, task 26.4’s batch): `design_spec.md` S-16 lists *"send a manual reminder to a user about an outstanding report"* among its controls and names FR-173 in its FR list, and neither identifier appeared anywhere in this file — an orphaned requirement, discovered because 26.4 built the screen that was supposed to carry it. It lands here rather than on its own number because a reminder is a notification raised on demand: it is 49.3’s dispatch path with a person pressing the button instead of a schedule, and it needs this task’s screens to be read | In-app notifications delivered and read from the browser | TODO |
| 50.1 | The notification store and read state | api | Per-user delivery records under RLS, with read/unread the state UC-167 changes | Notifications delivered and marked read through the API | TODO |
| 50.2 | S-26 notification centre | web | UC-165 … UC-167 on the Index archetype, with both empty states (§4.6) | In-app notifications read from the browser in three locales | TODO |
| **51** | **Email channel** | worker | The real ESP adapter (Mailjet, OQ-12) behind task 19's `EmailPort`, and every remaining category onto the outbox/worker path that verification email already uses; template wording ships as release catalogues, behaviour stays in the store (OQ-43) | Emails delivered through the outbox; no template sentence hardcoded in a `.ts` file | TODO |
| 51.1 | The SMTP adapter, provider by configuration | worker | One adapter speaking SMTP, with host, port and credentials from the environment — the shape both reference implementations already run (`EMAIL_HOST`/`PORT`/`USER`/`PASS` behind `nodemailer`), their connection, greeting and socket timeouts included, which exist because the outbox drain awaits each send while holding a row lock. **Mailjet (OQ-12) is the *configured* provider, not a compile-time one:** §12.5.2 requires the swap to be "a configuration change, not a code change", and SMTP is the one interface every candidate exposes — so a swap is an environment value rather than a new class, and no vendor type crosses the boundary because none enters it. **The swap set is bounded by NFR-27** (amended 25 Aug 2026, `non_functional_requirements.md` OQ-16 — sub-processors bound, no adequacy decision relied on), so `EMAIL_HOST` is a choice among permitted providers and not an open field: a deployment check asserts the configured host against that set, since a config-driven provider is exactly as easy to point somewhere non-compliant as somewhere compliant | Mail delivered through the configured provider; changing provider changes only the environment; the logging adapter still substitutes cleanly | TODO |
| 51.2 | Every remaining category onto the outbox path | worker | The categories 49.1 registered, dispatched the way verification email already is: at-least-once delivery with the idempotency key as the job id, giving effectively-once processing (T-5) | Every category delivered through the outbox; a dispatcher restart duplicates nothing | TODO |
| 51.3 | Template wording as release catalogues | pkg:i18n | OQ-43's split made concrete — the *text* ships in the release, the *behaviour* stays in the store. No template sentence in a `.ts` file, in any of the three locales, separately authored per NFR-23 | Every template string is a catalogue key; the expansion harness covers them | TODO |
| 51.4 | Delivery outcome and suppression | worker | **The gap no row in this file owned before 25 Aug 2026** — the parent row and its siblings cover only the outbound leg. UC-174 (FR-160, FR-170, FR-171) and FR-157: the provider's bounce and complaint webhook, normalised at the adapter onto one platform event vocabulary (§12.5.2's third rule), matched against `EmailDispatched.providerMessageId` — which task 19 put on the port saying "task 51 is where it acquires a consumer" — plus NFR-107's exponential retry bounded at 24 h and suppression of an address on its **first** hard bounce. **This is where a provider swap actually costs something**, since 51.1's transport is provider-neutral and an event feed is not; separating them is what makes that cost visible instead of discovered. Neither reference implementation carries this, and neither is asked to — no NFR-107 binds them | A hard bounce suppresses the address on first occurrence; a transient failure retries on the bounded schedule; the event vocabulary is provider-independent | TODO |
| **52** | **Notification preferences** | api+web | Per-user preferences — API and screens together | Preference opt-outs honoured end-to-end | TODO |
| 52.1 | Per-user preferences | api | UC-168 (FR-9, FR-163): what a user may turn off, bounded by 49.1's per-category opt-out flag — a category that may not be disabled must not render a toggle that lies | Preferences stored and read; a mandatory category cannot be disabled | TODO |
| 52.2 | Opt-out honoured at dispatch | api | Enforced where the notification is raised, beside 49.3, and not in the browser — a preference checked at render is a preference that still sent the email | An opted-out category is not dispatched, and the suppression is recorded | TODO |
| 52.3 | S-27 profile, language and notification preferences | web | UC-13, UC-14 and UC-168 (FR-9, FR-10, FR-64) on one Record screen. **UC-13 and UC-14's API half has no row of its own in this file** — display name, contact email, preferred interface language — and lands here with 52.1 because S-27 is the only screen that carries them. Whether that widens this task's API scope is for its batch, on task 23's precedent | S-27 live in three locales; a language change persists across sessions | TODO |

## Phase 7 — Billing (§15.4 #7)

| # | Name | Scope | Description | Expected result (deliverables) | Status |
| --- | --- | --- | --- | --- | --- |
| **53** | **Plan catalogue** | api+config | Plans as versioned config (DR-3) | A plan change lands as data, no redeploy | TODO |
| 53.1 | Plans as versioned records | api+config | UC-89 and UC-92 (FR-84, FR-87): a plan as a versioned first-class record, so changing price or entitlements does not restate live agreements — grandfathering is a property of the version, not a migration someone remembers to write | A plan versioned; existing subscribers stay on their version | TODO |
| 53.2 | Entitlements, quotas and pricing | api+config | UC-90 and UC-91 (FR-85, FR-86): what each plan grants, as data, priced per currency and billing cycle | A plan's grants and prices change as data, with no redeploy | TODO |
| 53.3 | Plan presentation copy | config | FR-61's other half — the text edited by people who cannot deploy, which OQ-43 kept in the store alongside help-centre articles. Task 76 is what serves it, and 74.6 is what wants to read it | Plan copy authored as store data in three locales | TODO |
| **54** | **Entitlement service** | api | Entitlements with the null implementation (AD-5), so `BILLING_ENABLED=false` still passes UC-17…48 (NFR-1) | Checks live at the seams; the task-18 billing-off CI job green against the full core suite | TODO |
| 54.1 | `EntitlementPort` and the null implementation | api | AD-5's null implementation is what makes `BILLING_ENABLED=false` a real configuration rather than a flag nobody exercises. **`check` takes one named object** — converted while the port still has no implementation, which CLAUDE.md records as the cheapest moment such a change will ever have | The port defined; the null implementation grants everything | TODO |
| 54.2 | Checks at the seams | api | The call sites where a quota bites: report creation, export, entity count, user count. **Enumerating them is the deliverable** — an entitlement check added wherever someone noticed is how a plan limit becomes advisory **Owes S-16's seat region (recorded 26 Aug 2026, task 26.4's batch):** §6.10 and UX-50 put seat consumption and an entitlement gate on that screen, and 26.4 shipped neither — UX-50 requires the limit, the allowance, current consumption and the upgrade path *in that order*, and only consumption is knowable without this port. The user-count seam named above is the same one. | Every entitlement-bearing action checks, against an enumerated list | TODO |
| 54.3 | The billing-off job green across the core suite | infra | Task 18's `BILLING_ENABLED=false` job has grown the UC-17 … UC-48 suite as it landed; this is where it must pass in full (T-1, NFR-1). It asserts the opposite of the billing-on job, and neither means anything alone | The billing-off job green against the whole core suite | TODO |
| **55** | **Invoice model and e-Factura port** | api | **The first billing sprint, per R-6 and the 1 Oct 2026 mandate:** the immutable invoice document model and `EInvoicingPort` behind a DI token (P-7) | Port and model merged; the port swappable without caller change | TODO |
| 55.1 | The immutable invoice document model | api | DR-8: a fiscal document is immutable, so the model has **no update path at any layer** — corrections are credit notes (FR-125), and building an editable model first is how immutability becomes a retrofit nobody completes | An issued invoice has no mutation path from domain to database | TODO |
| 55.2 | `EInvoicingPort` | api | Behind a DI token (P-7), swappable without caller change. **The first billing sprint**, per R-6 and the 1 October 2026 mandate — the port and the model land together because the deadline is external | The port merged with a stub adapter; no vendor type outside it | TODO |
| 55.3 | The port's recovery query | api | T-5: an adapter without provider-side idempotency **must declare a recovery query**, or a dispatcher restart duplicates an e-Factura submission. Declared as part of the port's contract here, rather than discovered at task 56 | The port cannot be implemented without one; a restart is provably safe | TODO |
| **56** | **e-Factura transmission** | api+worker | The transmission outbox as the buffer — an untransmitted invoice is a tracked exception with an owner (NFR-71), never a blocked checkout; integrated against the national test environment early | A submission round-trip against the test environment, through the outbox | TODO |
| 56.1 | The transmission outbox as the buffer | api+worker | NFR-71: an untransmitted invoice is a **tracked exception with an owner**, never a blocked checkout — the buffer is what decouples a statutory obligation from a customer's payment | A transmission failure leaves checkout unaffected and raises an owned exception | TODO |
| 56.2 | The national test environment | worker | Integrated **early**, because the deadline is external and the first integration surprise is the expensive one. §15.4's first scheduling fact says so in as many words | A submission round-trip against the test environment, through the outbox | TODO |
| 56.3 | Rejection and reissue | api+worker | UC-130 (FR-127): correcting and reissuing a rejected transmission — the path 56.1's exception queue exists to feed, and A-13 (68.7) is the screen that works it | A rejected transmission corrected and reissued, recorded at each step | TODO |
| **57** | **Fiscal numbering** | api | Gapless per-series-per-year numbering under the row lock (DR-8, AD-7, T-6); documents immutable — corrections by credit note, never edit (FR-125); fiscal dates as calendar date + timezone (NFR-34) | A concurrent-issuance test shows no gap and no duplicate, including after rollback | TODO |
| 57.1 | Series, year and the row lock | api | DR-8, AD-7, T-6: gapless per series per year under a row lock. Serialised issuance is accepted deliberately — at ≈200 documents/month it is four orders of magnitude from being a bottleneck, and the lock is what makes gaplessness survive | A number allocated under the lock, with series and year part of the key | TODO |
| 57.2 | Gaplessness under rollback | api | The property the lock exists for, and the only test that proves it: concurrent issuance **including a rolled-back transaction**. A sequence would pass a concurrency test and fail this one | The concurrent-issuance test green with rollback included; no gap, no duplicate | TODO |
| 57.3 | Fiscal dates | api | NFR-34: the fiscal year a series belongs to is a **calendar date plus its timezone**, never an instant. Encoding 31 December as an epoch value is how a document lands in the wrong fiscal year, and FR-125 makes that uncorrectable by editing | A document issued near midnight lands in the right fiscal year from any timezone | TODO |
| 57.4 | Corrections by credit note | api | UC-133 (FR-125): the only lawful way to change an issued invoice's effect, and the reason 55.1 has no update path | A credit note issued against an invoice; the original is untouched | TODO |
| **58** | **Order saga** | api | The saga with compensation — eventual convergence, not atomicity (T-4, NFR-59); its terminal `inconsistent` state a monitored metric with a work queue | Compensation test passes; the inconsistency metric visible | TODO |
| 58.1 | The saga steps and their compensations | api | T-4: NFR-59's "all-or-nothing" is eventual convergence with compensation, not atomicity — the requirement's own closing clause concedes as much. Each step declares its compensation where it is written, not afterwards | Every step has a compensation; a mid-saga failure converges | TODO |
| 58.2 | The `inconsistent` terminal state | api | T-4's residual exposure, and the slice that discharges it: the terminal state must be **a monitored metric with a real work queue**, or the concession becomes a silent failure | The metric visible; an inconsistent order appears in a queue with an owner | TODO |
| 58.3 | The compensation test | api | Each declared failure point exercised — the happy path proves nothing about a saga | Compensation proven at every declared step | TODO |
| **59** | **Payment adapter and first rail** | api | The single adapter surface for the payment rails (D-7, D-8 — Stripe is unavailable by fact, not choice), and the first rail integrated against its sandbox | Order → payment → entitlement e2e on the first rail | TODO |
| 59.1 | The adapter surface | api | One surface for all rails (D-7, D-8). Stripe is unavailable to Moldova-resident businesses **by fact rather than by preference**, which is why this stack exists at all and why the Stripe skills are deliberately not installed | The surface defined; no rail type outside its adapter | TODO |
| 59.2 | The first rail — domestic card | api | UC-116 and UC-117 (FR-114 … FR-116), 3-D Secure challenge included, against the acquirer's sandbox | Card payment e2e in the sandbox, strong authentication included | TODO |
| 59.3 | Order → payment → entitlement | api | The join: a settled payment provisions the entitlement 54.2 checks. Until this closes, the two halves are separately green and jointly unproven | The full path e2e on the first rail | TODO |
| **60** | **Remaining payment rails** | api | The remaining rails behind the same surface — swapping a rail changes no caller behaviour (L, P-7) | Each rail's sandbox e2e green behind the unchanged adapter surface | TODO |
| 60.1 | MIA instant payment | api | UC-120 (FR-114, FR-118): the national instant rail, settling within seconds, behind the unchanged 59.1 surface | MIA sandbox e2e; no caller changed (L, P-7) | TODO |
| 60.2 | Bank transfer against a proforma | api | UC-121 (FR-114, FR-119): the customer's own bank, no amount ceiling, settled asynchronously by 65.3's reconciliation rather than in the request. **The rail whose asynchrony is the largest behavioural difference**, and therefore the one most likely to leak into a caller | Bank-transfer e2e including the proforma; no caller changed | TODO |
| 60.3 | The merchant-of-record adapter, registered inactive | api | UC-122 (FR-114) is **MVP-inactive by D-8**, and UX-7's second exemption class names it: it gains a screen when the rail is activated, not before. Registering the adapter without activating it is the decided position, not a half-measure | The adapter registered and inactive; no screen, per UX-7 | TODO |
| 60.4 | Recurring charges | api | UC-123 … UC-125 (FR-120): the scheduled charge executed **idempotently**, a soft decline retried and a hard one not, and the customer notified of failure through 49.1's categories — involuntary churn from an unnoticed card failure is what this prevents. **No row in this file names FR-120**; it lands here because the stored token and the rail are the same adapter, and this task's batch should confirm that rather than inherit it | Recurring charge, retry policy and failure notice e2e | TODO |
| **61** | **Invoicing and VAT** | api | Invoice issuance on task 57's numbering; VAT rules as effective-dated config (AD-4) | A paid order yields a correct invoice; a VAT change lands as data | TODO |
| 61.1 | Invoice issuance | api | UC-126 and UC-127 (FR-121, FR-122): a proforma that creates **no** fiscal document, and the fiscal invoice generated on payment. The two are separated because only the second consumes 57.1's number, and issuing a number for a proforma is how a series gains a gap | A proforma requests payment without a fiscal document; payment yields a numbered invoice | TODO |
| 61.2 | Invoice delivery, recorded | api | UC-131 (FR-128): placing the invoice with the billing contact **and recording that it was placed**. The record is the requirement, because e-Factura transmission (task 56) and customer delivery are different obligations that fail independently | Delivery attempted, outcome recorded, and distinguishable from transmission | TODO |
| 61.3 | The billing audit ledger | api | Task 13's substrate applied: `CALL audit.enforce_append_only(...)` after creating the table **and its partitions**, because neither the TRUNCATE trigger nor RLS propagates to a partition. Task 13's row named ledger as one of its four tables and left it here | Ledger rows append-only by privilege, partitions included | TODO |
| 61.4 | VAT rules as configuration | api+config | UC-128 (FR-124) with effective dates (AD-4). A VAT change is a data change **with a date**, and that date is a calendar date, not an instant (NFR-34) | A VAT rate change lands as data and applies from its effective date | TODO |
| 61.5 | Foreign-currency invoices | api | UC-136 (FR-129): the BNM rate **on its date** fixes the MDL equivalent the fiscal return is built from. A rate date is a calendar date — this is the case CLAUDE.md's time rule is written for | An invoice in a foreign currency carries its rate and its rate date | TODO |
| **62** | **Metering** | api | Metering events onto task 13's append-only substrate: `CALL audit.enforce_append_only(...)` after creating the table **and its partitions**, since neither the TRUNCATE trigger nor RLS propagates to a partition. Instants are `timestamptz` in storage (OQ-50) | Metering rows written append-only by privilege | TODO |
| 62.1 | The metering table, its partitions, and append-only | api | The order matters and the parent row already says so: create the table **and its partitions**, then `CALL audit.enforce_append_only(...)`, because neither the TRUNCATE trigger nor RLS propagates to a partition. Instants are `timestamptz` in storage (OQ-50), converted to epoch-ms at the DTO boundary and never inward | Metering rows append-only by privilege; a partition is not a hole in the guarantee | TODO |
| 62.2 | Event capture at the seams | api | The same seams 54.2 enumerated, emitting rather than checking — **one list with two consumers**, so a metered action and a checked action cannot diverge | Every metered action emits; the list is provably 54.2's | TODO |
| 62.3 | Usage counters | api | UC-66 (FR-105): consumption against entitlement limits, read from 62.1 rather than counted live | Counters queryable per organization | TODO |
| **63** | **Checkout and billing screens** | web | Plans, checkout, invoice list and download for the tenant | A tenant subscribes and retrieves an invoice from the browser | TODO |
| 63.1 | S-18 plan comparison and selection | web | UC-96 … UC-98 on the Comparison archetype, reading 53.1 and 53.3 | Plans compared and selected in three locales | TODO |
| 63.2 | S-19 order and summary | web | UC-110 ... UC-112 (FR-110): net, VAT, gross and the available rails before committing -- the screen a customer decides on | The summary shows net, VAT, gross and every rail open to this customer | TODO |
| 63.3 | S-19 confirm, track and cancel | web | UC-113 ... UC-115 (FR-111 ... FR-113): confirming with **evidence of what was agreed** (where 75.1's consent decision binds a second time), following an asynchronous settlement to its conclusion, and withdrawing an unpaid order while no fiscal document exists | An order confirmed with the agreement recorded, tracked to settlement, and cancellable while unpaid | TODO |
| 63.4 | S-20 payment hand-off and return | web | UC-116 … UC-121 on Focus + Status: leaving for the rail and coming back, **including the returns that are not success**. Task 24's OAuth round trip is the nearest shape already in the codebase, notice surface included | Every rail's hand-off and return handled, failure paths designed rather than defaulted | TODO |
| 63.5 | S-21 payment instruments | web | UC-118 and UC-119 (FR-117): view, replace, remove and default a stored card, with consent to recurring billing an explicit act rather than a side effect of paying once | Instruments managed from the browser | TODO |
| 63.6 | S-22 invoices and documents | web | UC-132 (FR-128): invoice history and downloads **at any time**, including after a subscription lapses — D-13 and NFR-31 make the lapsed state the one that must work | Invoices listed and downloadable in every subscription state | TODO |
| 63.7 | S-23 billing account | web | UC-108: the billing contact and details an invoice is issued to | The billing account maintained from the browser | TODO |
| 63.8 | S-24 subscription status | web | UC-99 ... UC-103: the current state, cycle and renewal date on the Status archetype | Current subscription state legible at a glance | TODO |
| 63.9 | S-24 subscription history | web | UC-104 ... UC-107 on the Index half -- plan changes, grandfathering (53.1), and what a past cycle actually cost | Every past change listed with what it changed and when | TODO |
| 63.10 | S-17 plan, entitlements and usage | web | UC-65 and UC-66 (FR-90, FR-105) over 62.3: plan, entitlements, cycle, renewal date, and consumption against the limits — visible **before** a quota bites, which is the difference between a plan and a surprise | Usage against entitlements visible ahead of the limit | TODO |

## Phase 8 — Operations (§15.4 #8)

| # | Name | Scope | Description | Expected result (deliverables) | Status |
| --- | --- | --- | --- | --- | --- |
| **64** | **Core/billing reconciliation** | worker | The nightly job reporting orphaned billing records as an operational metric — the price of no cross-schema FK (T-2) | Job runs on schedule; the orphan metric visible | TODO |
| 64.1 | The nightly job | worker | T-2's price paid: with no foreign key from `billing` to `core.organization` — the physical expression of NFR-15, and what makes NFR-1's "disable billing entirely" possible at all — orphans exist, and this job is the mitigation promised when the FK was declined | The job runs on schedule and finds seeded orphans | TODO |
| 64.2 | The orphan metric | worker | Reported as an **operational metric**, because a mitigation nobody looks at is not one | The metric visible, non-zero against a seeded orphan | TODO |
| **65** | **Collections** | api | The collections flow | Collections e2e | TODO |
| 65.1 | The dunning sequence | api | UC-141 (FR-135): an overdue invoice escalated through **configured** reminders — the schedule is data (AD-4), like every other behaviour, and 49.1's categories carry the messages | A dunning sequence runs from configuration and changes without a redeploy | TODO |
| 65.2 | Restriction after grace | api | UC-142 (FR-104, FR-136): suspend **without deleting anything**. The word is load-bearing — D-13 and NFR-31 require export to keep working from a lapsed tenant, so restriction may not touch data | Service restricted, nothing deleted, export still available | TODO |
| 65.3 | Statement import and automatic match | api | UC-137 and UC-138 (FR-131, FR-132): bringing settlement data in, and matching, settling and provisioning it without manual work -- the path 60.2's bank transfer settles through | An imported statement settles matching invoices with no operator action | TODO |
| 65.4 | Reconciliation exceptions | api | UC-139 and UC-140 (FR-133, FR-134): the unmatched or partial payment worked **with recorded rationale**, and the manual mark-paid. The exception is where operator time actually goes, which is why it is its own slice and A-10 is its own screen | Exceptions reach a queue with an owner; every resolution carries its reason | TODO |
| **66** | **Refunds** | api | Refunds through the saga | Refund e2e with compensation | TODO |
| 66.1 | Refund through the saga | api | UC-145 … UC-147 over 58.1's compensation, under the same recovery-query obligation 55.3 declared — a dispatcher restart must not duplicate a refund (T-5) | Refund e2e with compensation; a restart duplicates nothing | TODO |
| 66.2 | The credit note a refund issues | api | 57.4's mechanism applied: a refund changes an issued invoice's effect, which is a credit note and never an edit (FR-125) | A refund produces a credit note against the original | TODO |
| 66.3 | Chargebacks | api | The involuntary case: it arrives without a request, against a document that may already have been transmitted to e-Factura (task 56), so the correction path is 56.3's rather than a local reversal | A chargeback recorded, reconciled, and its fiscal consequence handled | TODO |
| **67** | **Admin console: platform screens** | admin | The platform half of A-01…A-18 made functional against the public API, actors.md RBAC (BO, PA); the `compact` density is **blocked on architecture.md OQ-44** and must not be closed by app-local tokens (UX-127). **Owns three task-23 deferrals:** the real console chrome replaces `realm/session-strip.tsx`, A-01's exit refines to "the console home for the operator's privilege level" (every sign-in lands on A-02 meanwhile), and UC-87's account management (FR-80) supersedes the `admin:provision` CLI as the lockout release | Platform screens functional; OQ-44 either closed upstream or the divergence still stated | TODO |
| 67.1 | The console chrome | admin | Task 23's first deferral: the real chrome replaces `realm/session-strip.tsx`, and **A-01's exit refines to "the console home for the operator's privilege level"** — every sign-in lands on A-02 meanwhile, which is a recorded interim rather than the design | The real chrome on every screen; the interim strip deleted | TODO |
| 67.2 | The `compact` density | pkg:ui | **Blocked on `architecture.md` OQ-44, and the block is the deliverable.** §11.4 specifies two density modes over one token set and §12 makes `compact` the administrative default, but `tokens.css` defines a single scale. `apps/admin` already declares the `[data-density="compact"]` hook and authors no values **on purpose** — a second token file in an app is what UX-127 calls a defect, and §15.4 moved the cascade into `packages/ui` to prevent one. Entangled with `design_spec.md` OQ-13: `wide` and `extra` have no pixel values either | The density steps in `packages/ui` — or OQ-44 still open and the divergence from §12 still stated, never closed by app-local tokens | BLOCKED |
| 67.3 | A-02 organization register | admin | UC-69 on the Index archetype — the screen every admin sign-in currently lands on **Owns `AdminRealmGuard`** (assigned 27 Aug 2026, project owner, closing a plan gap task 28.2 surfaced): `apps/api/CLAUDE.md` listed it among the edge guards not built and no row owned it. It stays here rather than in task 28 because it guards nothing until this screen: the only admin routes today are task 23's sign-in handshake, already gated by a sealed cookie, an Origin proof and mandatory TOTP, so building it earlier would be a guard with no unguarded route to protect | The register live under actors.md RBAC (PA), reached through `AdminRealmGuard` rather than past it | TODO |
| 67.4 | A-08 admin accounts and system audit log | admin | UC-87 and UC-88 (FR-80, FR-81). Task 23's third deferral: **this supersedes the `admin:provision` CLI as the lockout release**, and until it ships the CLI is the only way an admin account comes into existence **Owns `AuditInterceptor`** (assigned 27 Aug 2026, project owner, closing a plan gap task 28.4 surfaced): `architecture.md` §6.2 puts it fourth in the request pipeline and FR-159 gives it scope, but no row claimed it. It waits for this screen because FR-159's remaining coverage is a question about *what the log must contain*, and that is answerable only once A-08 exists to read it — per-field tenant mutations are already captured by task 14's trigger onto `core.field_change`, so an interceptor built earlier would mostly duplicate that with less detail. Task 28.4 writes the admin sign-in events directly, since a platform event with a known shape needs no general mechanism | Admin accounts managed in the console; the CLI is no longer the only path; every state-changing action attributed through `AuditInterceptor` (FR-159) | TODO |
| 67.5 | A-03 content and translation console | admin | UC-71 … UC-74 (FR-62): the editor and publish flow for the content task 76 serves. All three locales authored here, **none machine-translated** (NFR-23), with editorial sign-off recorded per locale per publication | Content authored, translated and published in three locales | TODO |
| 67.6 | A-04 taxonomy versions and mappings | admin | UC-75 ... UC-77 (FR-65 ... FR-67) over 33.1 and 78.2 -- registering a version and mapping it to its predecessor | A version registered and mapped from the console with no deploy | TODO |
| 67.7 | A-04 migration runs | admin | UC-78 and UC-79 (FR-68, FR-69). NFR-86 is the constraint and 73.6's rehearsal is the proof: a backwards-compatible rollout needs **no code release**, and a breaking change is confined to mapping content rather than schema change | A rollout executed from the console over 33.3's two-version setup, zero code releases | TODO |
| 67.8 | A-05 factor sets, thresholds, validation rules | admin | UC-80 … UC-82 over 37.1 and 41.1 — the editor that makes AD-4 real for the people who own the standard rather than the code | A factor set and a validation rule edited and published from the console | TODO |
| 67.9 | A-07 support access request and audit log | admin | UC-85 and UC-86 (FR-77 … FR-79): scoped, **expiring** access, and the log that holds the privilege accountable. The log is the third table task 13's row named — it applies `audit.enforce_append_only(...)` like the others | Time-boxed access granted and expiring; the log append-only by privilege | TODO |
| 67.10 | A-17 notification categories and templates | admin | UC-176 over 49.1: behaviour edited as store data, wording as a release catalogue (OQ-43). **The split has to be visible on the screen**, or an operator will expect to edit text they cannot | Categories edited from the console; the wording boundary legible to the operator | TODO |
| 67.11 | A-18 identity provider configuration | admin | UC-70 over task 24's provider store rows. **FR-82's rotate-without-redeploy is a recorded deferral until OpenBao** — the client secret stays in the environment — so this screen edits behaviour and not the secret, and must say which is which rather than imply it edits both | Provider behaviour edited without a deploy; the secret's location stated on the screen | TODO |
| **68** | **Admin console: billing screens** | admin | The billing half of A-01…A-18 | Billing screens functional under RBAC | TODO |
| 68.1 | The exception-queue archetype | pkg:ui | Four of this task's screens are the same archetype — A-10, A-11, A-13, A-14 — so it is built once as inventory with its state set (UX-8, UX-89, UX-90) before any of them, not extracted from the fourth | The archetype in `packages/ui` with its states designed | TODO |
| 68.2 | A-09 plan definition and versioning | admin | UC-89, UC-90 and UC-92 (FR-84, FR-85, FR-87) over 53.1. The screen must make version-and-grandfather legible, or an operator will price-change a live agreement believing they are editing a draft | Plans defined and versioned; grandfathering visible before the change is saved | TODO |
| 68.3 | A-09 pricing and discounts | admin | UC-91 and UC-93 ... UC-95 (FR-86) over 53.2 -- per currency and billing cycle, discounts included | Prices and discounts edited as data, no redeploy | TODO |
| 68.4 | A-12 invoices | admin | UC-126 ... UC-129, UC-131, UC-132 and UC-136 over task 61 -- issuance, delivery, and the FX rate with its date | Invoices listed, inspectable, and reconciling with the ledger | TODO |
| 68.5 | A-12 credit notes | admin | UC-133 (FR-125) over 57.4: the only lawful way to change an issued invoice's effect. The screen must not offer an edit, because 55.1 has no update path to offer | A credit note raised against an invoice; no edit affordance exists | TODO |
| 68.6 | A-12 numbering series | admin | UC-134 and UC-135 (FR-123, FR-130) over 57.1: series kept **gapless and monotonic**, plus the statutory six-year archive. **No offered action may create a gap** -- the database refuses it, and an affordance that always errors is a defect in the screen | Series managed and archived; no offered action can create a gap | TODO |
| 68.7 | A-13 e-Factura transmission exceptions | admin | UC-130 over 56.1's queue — NFR-71's "tracked exception with an owner" is, concretely, this screen | Rejected transmissions worked to resolution | TODO |
| 68.8 | A-10 reconciliation workspace | admin | UC-137 … UC-140 over 65.3 | Unmatched and partial payments worked with recorded rationale | TODO |
| 68.9 | A-11 collections and dunning | admin | UC-141 … UC-144 over 65.1 and 65.2 | Dunning state visible and actionable | TODO |
| 68.10 | A-14 refunds and chargebacks | admin | UC-145 … UC-147 over task 66 | Refunds and chargebacks worked from the console | TODO |
| 68.11 | A-16 revenue and VAT export | admin | UC-160 ... UC-162 over task 61's VAT rules -- the export a fiscal return is built from | Revenue and VAT exportable for a period | TODO |
| 68.12 | A-16 the billing audit ledger | admin | UC-163 and UC-164 over task 61's append-only ledger. The numbers must **reconcile with the ledger** rather than be recomputed beside it -- a second computation is a second answer | The ledger readable and reconciling with the revenue figures | TODO |
| **69** | **Adoption metrics** | api+admin | Metrics off the metering substrate, surfaced in the console | Metrics queryable in the console | TODO |
| 69.1 | Metrics off the metering substrate | api | UC-83 (FR-83) over 62.1: the defined MVP success metrics computed from metering rather than from a second counter kept beside it | The defined metrics queryable | TODO |
| 69.2 | A-06 adoption metrics | admin | UC-83 and UC-84 on the Dashboard archetype — including the **export**, which is what produces the evidence for the Phase 2 go/no-go decision. That is what the metrics are for | Metrics visible and exportable from the console | TODO |
| **70** | **Enterprise contract path** | api+admin | The Enterprise contract flow | Enterprise flow e2e | TODO |
| 70.1 | The Enterprise request API | api | UC-153's request half (FR-142) | A request raised and retrievable | TODO |
| 70.2 | S-25 Enterprise request | web | The tenant's Focus screen, reached from 63.1's plan comparison where the plan is not self-serve | A request raised from the browser in three locales | TODO |
| 70.3 | A-15 Enterprise quotes | admin | UC-153 ... UC-156 -- raising and revising a quote against Enterprise terms | A quote raised, revised and issued from the console | TODO |
| 70.4 | A-15 Enterprise contracts | admin | UC-157 ... UC-159 -- the contract a quote becomes, and what it entitles (54.2) | A contract concluded and its entitlements provisioned | TODO |
| **71** | **Edge and topology** | infra | Caddy edge and the EU/EEA VM topology | The staging stack reachable through the edge | TODO |
| 71.1 | The Caddy edge | infra | TLS, headers, and **the rate limits §12.5.6 assigns to the edge rather than to the application**. Task 19's build-log entry already records enumeration on the registration route as bounded here, so this is where a recorded assumption becomes true | The edge enforces the limits §12.5.6 names; task 19's assumption is discharged | TODO |
| 71.2 | The EU/EEA VM topology | infra | The three-VM split, which buys durability and **not** uptime (T-7) — and the fiscal archive on a *different provider from compute* (§12.5), which is what makes provider-independence true by construction rather than by claim | The topology stood up; the archive provably provider-independent | TODO |
| 71.3 | Staging through the edge | infra | The stack reachable end to end — the target task 72 deploys onto | Staging reachable through the edge | TODO |
| **72** | **Deploy pipeline** | infra | Ansible/OpenTofu deploy from CI images; backup and restore | A staging deploy from CI; a restore actually rehearsed, not assumed | TODO |
| 72.1 | OpenTofu — the infrastructure | infra | 71.2's topology as code, so the staging stack and the production one are the same declaration with different variables | The VMs and networking reproducible from the repository | TODO |
| 72.2 | Ansible — the hosts and the deploy | infra | CI images deployed onto them. Task 18's build-log entry records the ordering fact this must honour and calls it "an ordering fact for task 72's deploy, not a CI detail": **migrate with the image before starting it** | A staging deploy from CI images, migrated in the right order | TODO |
| 72.3 | GHCR publishing | infra | Task 18's recorded deferral — magnamed's `docker` job is the model named there, to be taken wholesale rather than re-derived | Images published and pulled by the deploy | TODO |
| 72.4 | Backup | infra | Onto 71.2's provider-independent archive, at the NFR-51 objectives, with `synchronous_commit` scoped where RPO = 0 is required (T-9) | Backups taken, verifiable, and meeting NFR-51 | TODO |
| 72.5 | Restore, rehearsed | infra | NFR-52, and its evidence class is **D — demonstration**, not test. A restore that has never been run is a plan; the build order's own wording is "actually rehearsed, not assumed" | A restore performed and timed against NFR-51 | TODO |
| **73** | **Runbooks and rehearsals** | docs | `docs/runbooks/` as deliverables — six NFRs are verified by rehearsal, not by test | Each runbook written and its rehearsal executed and recorded | TODO |
| 73.1 | Host-loss recovery (NFR-48) | docs | Loss of `edge`, `redis` or VM-1 is excluded from the availability percentage and bounded by an RTO instead (T-7). Failover is **a manual runbook by decision** (T-8) — no Patroni, no repmgr, no VIP switch — so the runbook *is* the mitigation, and its currency is the residual risk | The runbook written and the recovery rehearsed against the stated RTO | TODO |
| 73.2 | Restore, including provider-independent (NFR-52, OQ-14) | docs | Quarterly restore rehearsal, **and one per year restoring to a non-Hetzner host from the object store alone**. `non_functional_requirements.md` OQ-14 stays open until that has been performed and recorded — planned-for is not evidenced | Both rehearsals performed and recorded; OQ-14 closed by evidence rather than by argument | TODO |
| 73.3 | Data-subject requests (NFR-28, NFR-29) | docs | Access, rectification, erasure and portability within 30 calendar days, with statutory retention taking precedence (FR-130) and the retained categories reported back to the requester. Rehearsed annually against a seeded tenant | The runbook written and rehearsed; retained categories reported | TODO |
| 73.4 | Exit from a lapsed tenant (NFR-31) | docs | D-13: full customer data export in an open format **irrespective of subscription state**, rehearsed from a lapsed tenant — the state where it is most likely to be broken and least likely to be exercised | The exit rehearsed from a genuinely lapsed tenant | TODO |
| 73.5 | Publish and revert (NFR-85) | docs | A configuration-only change to production within one working day, **revertible in a single step**, timed. This is the rehearsal that proves AD-4 rather than asserting it | A timed publish-and-revert against production configuration | TODO |
| 73.6 | Taxonomy and template rollout (NFR-86) | docs | Both a compatible **and a breaking** rollout rehearsed in staging over 33.3's two-version setup, with the breaking change confined to mapping content: zero code releases, zero schema changes | Both rollouts rehearsed and recorded | TODO |

## Phase 9 — Public tier (§15.4 #9)

**Appended 24 Aug 2026, and the cause was fixed at source in the same change.** These four were
missing because §15.4's build order ran to **eight** steps and none of them was the unauthenticated
surface — so a plan sliced from §15.4, which is what this file is, could not contain the public
tier and never did. `design/IMPLEMENTATION_PLAN.md` carried it all along as Phase 10, the only one
of its twelve MVP phases with no task against it (Phase 12 is post-MVP by its own heading and
correctly has none), while §15.4 claimed to sequence "the interface half of each step". Both
statements were locally true and together implied a coverage that did not exist. **§15.4 now carries
a ninth step**, so the next re-slice of this file inherits the tier instead of losing it again.

Meanwhile the routes have existed under `apps/web/src/app/[locale]/(public)/` since task 4 and every
one of them returns `null`; the prototypes (`EasyESG Public Home`, `EasyESG Public Legal`,
`EasyESG Help Centre`) have existed since task 1.

**Task 75 is a precondition of task 48**, and that is now §15.4's third scheduling fact rather than
this file's opinion: a step's position in the build order is a dependency statement, not a date, and
the legal slice binds at the pilot. The other three tasks are genuinely movable — Phase 10 records
that the public tier "depends on nothing from phases 4–9" and that "nothing later depends on them
either", and `web-public-is-a-leaf` is the CI rule holding that true.

**Both blocking questions closed 24 Aug 2026, and these rows now cite identifiers rather than prototypes.**
`design_spec.md` **OQ-12** closed by numbering the screens `S-29 … S-34`, registering the **Visitor (`VI`)** as an
MVP actor and writing `UC-177 … UC-182` — so UX-7 needed no exemption and §5 states content, controls and states
for every one of them. `architecture.md`'s shadowed **OQ-44** closed on **`platform/content`**, a module of its
own. What each task must still raise in its own batch is named in its row. **`design_spec.md` OQ-17 was raised and
closed the same day** — the three reading surfaces fit none of §4.6's nine archetypes, so §4.6 gained a tenth,
**Content**, derived from the delivered prototypes; nothing here is blocked on it. Three questions stay open on
purpose: `architecture.md` **OQ-31** (host split — task 74 builds under its stated, reversible assumption),
`design_spec.md` **OQ-16** (whether the cookie choice is recorded — task 75) and `design_spec.md` **OQ-18** (how
many legal documents the set holds — the prototype draws five, every normative source says three — task 75).

**The states passes are these tasks' own work, under UX-90.** `EasyESG Public Home`, `Public Legal` and
`Help Centre` carry no states-pass artboard where `Identity` and `Workspace` do. That is an implementation
obligation on tasks 74, 75 and 77 — every applicable §8.1 state designed before the screen is built — not a
precondition anyone else owes them.

| # | Name | Scope | Description | Expected result (deliverables) | Status |
| --- | --- | --- | --- | --- | --- |
| **74** | **Public shell and marketing home** | web | **S-29** (marketing home, UC-177) and **S-30** (legal set navigation shared with task 75), for VI. The `(public)` group made real: the marketing home at `/{locale}`, the public header and footer chrome, and §14.2's `"use cache"` exception — legal in this zone and prohibited in every other, because a compiler-generated cache key that knows nothing of `organization_id` leaks a rendered page across tenants **above** AD-2's boundary, where none of its probes look. Built under `architecture.md` **OQ-31**'s stated, reversible assumption (the home holds the locale root as the SEO landing page; if a host split is confirmed, `/home` becomes `/` behind a redirect) — the task proceeds under it and does not settle it. Adding a route means adding it to `UNAUTHENTICATED_SEGMENTS` in `proxy.ts`: route groups are invisible in URLs, so the auth boundary is a list whose default is closed. **Unknowns for the batch:** how three locales are addressed to a search engine, since `localePrefix: 'as-needed'` serves Romanian unprefixed and no NFR governs canonical or `hreflang`; and whether the home's pricing section ships before the plan catalogue (task 53) and the FR-61 read path (task 76) exist, and against what if so | Marketing home live in three locales at UX-73's 1440 / 834 / 390; the +40% expansion harness and the axe scan pass on it; `"use cache"` present in `(public)` and nowhere else; `web-public-is-a-leaf` still green | TODO |
| 74.1 | The `(public)` chrome and the auth boundary | web | The public header and footer, and the routes added to `UNAUTHENTICATED_SEGMENTS` in `apps/web/src/proxy.ts`. **Route groups are invisible in URLs, so the auth boundary is a list whose default is closed** — forgetting the list is how a public page becomes a redirect to sign-in | The chrome renders on every `(public)` route; `web-public-is-a-leaf` still green | TODO |
| 74.2 | The `Content` archetype | pkg:ui | §4.6's tenth, closed 24 Aug 2026 by `design_spec.md` OQ-17 and derived from the delivered prototypes (OQ-10) — values **extracted through `packages/ui`, never copied as markup**. Its states pass is this task's own work under UX-90, since the public prototypes carry no states-pass artboard where `Identity` and `Workspace` do | The archetype in `packages/ui` with its state set; no value copied out of a prototype | TODO |
| 74.3 | S-29 marketing home | web | UC-177, for VI, at UX-73's 1440 / 834 / 390. Built under **`architecture.md` OQ-31**'s stated, reversible assumption — the home holds the locale root as the SEO landing page, and if a host split is confirmed `/home` becomes `/` behind a redirect. The task proceeds under the assumption and **does not settle it**; the cost of reversing rises from the moment the home is real | The home live in three locales at all three widths; expansion harness and axe scan pass | TODO |
| 74.4 | `"use cache"`, here and nowhere else | web | §14.2's exception, **legal in this zone and prohibited in every other**: a compiler-generated cache key knows nothing of `organization_id`, so a cached tenant page leaks **above** AD-2's boundary, where none of its probes look. The proof of absence is as much the deliverable as the presence | `"use cache"` present in `(public)` and provably absent elsewhere, as a gate | TODO |
| 74.5 | Addressing three locales to a search engine | web | **An unknown to raise in this task's batch, not to answer in code.** `localePrefix: 'as-needed'` serves Romanian unprefixed and no NFR governs canonical or `hreflang`. However it is settled, it is written into the artefact that owns it before any `<head>` is authored | The decision recorded, then implemented — not a `<head>` someone guessed at | TODO |
| 74.6 | The pricing section | web | **The batch's second unknown:** whether the home's pricing ships before the plan catalogue (53.3) and the FR-61 read path (task 76) exist, and against what if so. Hardcoding prices here creates a second source of truth for the thing 53.1 versions on purpose | Either the section reads task 76's published copy, or it is deferred with the reason recorded | TODO |
| **75** | **Legal documents and the cookie choice** | web | **S-30** (UC-178) and **S-31** (UC-179) — terms of service, privacy notice, cookie policy and the cookie choice, drawn as the four artboards of `EasyESG Public Legal.dc.html`, one shared layout treating the three documents as a set with a plain-language summary above the formal text. **This slice must land before task 48** and the reason is not scheduling preference: `SiteFooter` has rendered `/legal/terms`, `/legal/privacy` and `/legal/cookies` on every identity screen since task 20 and all three return `null`, so three dead links ship today; and NFR-5's instrument — **Law No. 195/2024, applicable 23 August 2026** — binds the registration path task 19 already ships, while task 48 is where real SMEs arrive. **Three unknowns to raise in one batch, none answerable by a screen:** where the legal text lives, since FR-61 as narrowed by `architecture.md` OQ-43 names help-centre articles and plan presentation copy and **not** legal documents, and a terms change carries a version and an effective date wherever it lives; whether cookie consent is **recorded** or implied — **OQ-16**, which S-31's entry names as giving the screen two different postconditions, and recording it needs an API and a schema, not a checkbox; what the application actually sets, because the prototype's "What we do not set" is a factual claim about shipped code; and **OQ-18** — whether the set is three documents or the five the prototype's tab strip draws, the extra two being a data processing agreement and a sub-processor list, both of which commit the platform to maintaining them | The three documents and the cookie choice live in three locales; `SiteFooter`'s links resolve; the legal text's home and the consent decision are each written into the artefact that owns them **before** the screens are built | TODO |
| 75.1 | The three decisions, written before any screen | docs | **This slice produces no screen, and that is the point** — the parent row requires each decision written into its owning artefact *before* the screens are built. (i) **Where the legal text lives**: FR-61 as narrowed by `architecture.md` OQ-43 names help-centre articles and plan presentation copy and **not** legal documents, and a terms change carries a version and an effective date wherever it lands. (ii) **`design_spec.md` OQ-16** — consent **recorded** or implied; recording it needs an API and a schema, not a checkbox, and S-31's entry names this as giving the screen two different postconditions. (iii) **`design_spec.md` OQ-18** — three documents or the five the prototype's tab strip draws, the extras being a data processing agreement and a sub-processor list, each committing the platform to maintaining it | Three register rows read *Closed — < decision >* with authority and date, and the normative text each changes amended in the same edit | TODO |
| 75.2 | What the application actually sets | web | The prototype's "What we do not set" is **a factual claim about shipped code**, so it is verified against the code rather than transcribed. Task 22's session cookie, task 24's OAuth transaction cookie and task 23's admin realm cookie are the population as of today | An enumerated, verified inventory of every cookie the application sets and what for | TODO |
| 75.3 | S-30 the legal set | web | One shared layout treating the three documents as a set, with a plain-language summary above the formal text and the peer strip 74.2's archetype provides. **`SiteFooter` has linked to `/legal/terms`, `/legal/privacy` and `/legal/cookies` on every identity screen since task 20 and all three return `null`** — three dead links ship today, which is the concrete reason this precedes task 48 | The layout live; `SiteFooter`'s links resolve | TODO |
| 75.4 | The documents in three locales | web | UC-178, separately authored per NFR-23 — **never machine-translated**, and legal text is where that rule is least negotiable | Terms, privacy notice and cookie policy live in RO, EN and RU | TODO |
| 75.5 | S-31 the cookie choice | web | UC-179 on the Focus archetype, with 75.1's OQ-16 decision determining whether the choice is recorded or implied — and carrying its API and schema if recorded | The choice live in three locales and honoured by what the application actually sets | TODO |
| **76** | **Published content read path** | api | **`platform/content`** — the 36th module, decided 24 Aug 2026 by the shadowed OQ-44. FR-61's content — help-centre articles and plan presentation copy — readable by an unauthenticated client. The gap is narrow and total: task 16 built the generic store, tasks 33, 37, 40, 49 and 53 add rows to it, A-03's editor is task 67 (FR-62, UC-71, UC-72), and **nothing serves what has been published**. `apps/web` may not read the store around the API (DR-11, AD-9), so this is the read half of config-as-data. Published-only by construction rather than by a filter — `config.entry_schedule` is what is in force (task 16), so a draft has no address. Per locale, with FR-64's fallback reporting, which applies to FR-61 content only: catalogue gaps fail the build instead. **The module question is closed:** `platform/content`, not `platform/configuration` — which owns the store mechanism and knows nothing of any artefact's meaning — and not `platform/localization`, which resolves keys from committed catalogues rather than store rows. §6.7 and `CLAUDE.md` carry it. **Remaining unknowns for the batch:** whether the route is rate-limited as an auth path or as generic unauthenticated traffic (§12.5.6 — OQ-53 and OQ-55 are the precedent for naming a route rather than letting it inherit); and whether FR-61 rows are seeded here or by task 67 | A published article and a plan description readable in three locales through the public API; an unpublished version has no address; the routes appear in `packages/contracts/openapi/v1.json` and pass `openapi:check` | TODO |
| 76.1 | `platform/content` and the published-only read | api | The 36th module, decided 24 Aug 2026 by `architecture.md`'s shadowed OQ-44 — **not** `platform/configuration`, which owns the store mechanism and knows nothing of any artefact's meaning, and **not** `platform/localization`, which resolves keys from committed catalogues. Published-only **by construction rather than by a filter**: `config.entry_schedule` is what is in force, so a draft has no address to leak from | A published article readable unauthenticated; an unpublished version has no address at all | TODO |
| 76.2 | Per-locale reads with FR-64 fallback | api | The fallback applies to **FR-61 content only** — a catalogue gap fails the build instead, which is the opposite policy and deliberately so | Content in three locales; a missing translation falls back and is reported | TODO |
| 76.3 | The routes in the contract | api+pkg:contracts | `packages/contracts/openapi/v1.json` and `openapi:check` green, generated types regenerated and diffed. **`apps/web` may not read the store around the API** (DR-11, AD-9) — this is the read half of config-as-data, and a Server Component querying Postgres directly would be the privileged back door AD-9 forbids | The routes in the emitted spec; the generated types diffed by the same gate | TODO |
| 76.4 | Rate-limit classification | api | **An unknown for this task's batch:** whether the route is limited as an auth path or as generic unauthenticated traffic (§12.5.6). OQ-53 and OQ-55 are the precedent for naming a route rather than letting it inherit | The classification recorded in §12.5.6 and enforced | TODO |
| 76.5 | Who seeds FR-61's rows | api+config | **The second unknown:** seeded here, or authored through A-03 at 67.5. It decides whether `config/seed/` grows a content fixture or the store starts empty in every environment | Decided, recorded, and the chosen path implemented | TODO |
| **77** | **Help centre and write to support** | web | **S-32** (UC-180), **S-33** (UC-181) and **S-34** (UC-182), for VI and CA. The four artboards of `EasyESG Help Centre.dc.html` — the help centre as guest and as a signed-in user, the article, and write to support — reading task 76's content. **UX-109**'s Consistent Help (WCAG 2.2 3.2.6) puts help in the same place on every screen, so this task owns that placement across both the public chrome and the authenticated one. **"Write to support" is a blocking unknown and is raised before the task starts, not inside it:** no FR and no use case creates a support request. UC-85's precondition is that "a support request exists with a ticket reference", which presupposes a channel none of the seven documents specifies — so the candidates are an address the screen publishes, a form posting to the API and dispatching through task 15's outbox as verification email already does, or an external helpdesk, and those are three different products with three different data-protection footprints. The Scope column reads `web` provisionally and widens to `api+web` if the channel turns out to be a form — the same correction task 23's batch made to its own row, recorded rather than discovered mid-build. **S-32 settles the guest/signed-in question**: one screen with a session-dependent shell, not two — the same relationship S-01 holds across its three artboards — which `web-public-is-a-leaf` makes a real constraint, since `features/public` may not import an authenticated feature. Remaining for the batch: S-33's states pass, which the Help Centre prototype does not carry (UX-90) | Articles browsable and readable in three locales as guest and as a signed-in user; help reachable from the same place on every screen; the support channel decided and recorded before it is built; expansion harness and axe scan pass | TODO |
| 77.1 | The support channel, decided and recorded | docs | **Blocking, and raised before the task starts rather than inside it.** No FR and no use case creates a support request, yet UC-85's precondition is that "a support request exists with a ticket reference". The candidates — an address the screen publishes, a form posting to the API and dispatching through task 15's outbox as verification email already does, or an external helpdesk — are **three different products with three different data-protection footprints**. The parent's Scope reads `web` provisionally and widens to `api+web` if the answer is a form, which is the correction task 23's batch made to its own row rather than discovering mid-build | The channel decided and written into the artefact that owns it; the scope correction recorded if it applies | TODO |
| 77.2 | S-32 help centre | web | UC-180 on the Index archetype, reading 76.1. **One screen with a session-dependent shell, not two** — the same relationship S-01 holds across its three artboards — which `web-public-is-a-leaf` makes a real constraint, since `features/public` may not import an authenticated feature | The help centre browsable as guest and signed in, from one implementation | TODO |
| 77.3 | S-33 help article | web | UC-181 on 74.2's `Content` archetype. **Its states pass is this task's work under UX-90** — the Help Centre prototype carries none | An article readable in three locales; every applicable §8.1 state designed before it was built | TODO |
| 77.4 | S-34 write to support | web | UC-182 on the Focus archetype, built to whatever 77.1 decided — and only then | Support reachable by the decided channel, in three locales | TODO |
| 77.5 | UX-109 — consistent help | web | WCAG 2.2 3.2.6 puts help in the **same place on every screen**, which makes this task the owner of one placement across both the public chrome (74.1) and the authenticated one (30.1) — the only task in this file that reaches into both | Help reachable from the same location on every screen in both chromes | TODO |

## Phase 10 — Comprehensive Module (§15.4 #3, additive)

**Appended 25 Aug 2026, when `problem_overview.md` OQ-12 promoted C1–C9 into MVP scope, commercial tier included.**
Numbers are appended and the document's order is not the build order here: **task 78 lands with task 31 and must precede
task 36**, because D-A's report-level scope flag drives form UI, validation and export and P-11 puts what is expensive to
retrofit on day one. Task 79 follows task 36, task 80 follows 41 and 46, task 81 follows 53, 54, 63 and 67.

**What the promotion found, and it is why this phase is small rather than a second reporting core.** The *mechanism* was
already MVP and only the *content* was deferred: D-A makes Basic vs Comprehensive a report-level flag, NFR-2 mirrors
element names for B1–B11 **and C1–C9**, and FR-177 deferred the nine disclosures precisely because that made them
"additive rather than a rework". Neither D-A's flag nor NFR-2's mirroring had a row in this file — MVP work, unplanned,
and the reason the design looked out of scope when it was not. Task 78 is that gap; the rest is content on top of it.

**Nothing here invents a parallel mechanism.** Conditional applicability, validation, autosave, prior-period carry-forward
and both export formats are B1–B11's, driven by the scope flag rather than duplicated (FR-27 … FR-32). A slice below that
finds itself needing a second one has found a defect in task 78, not a requirement.

| # | Name | Scope | Description | Expected result (deliverables) | Status |
| --- | --- | --- | --- | --- | --- |
| **78** | **Comprehensive scope flag and the C1–C9 taxonomy** | api | **Lands with task 31 and must precede task 36.** D-A's report-level flag plus NFR-2's element-name mirroring — the half of the promotion that was already MVP and unowned | The flag drives what a report contains; C1–C9 exist as taxonomy data before any form does | TODO |
| 78.1 | The report-level scope flag | api | D-A, on task 31's report: `basic` or `basic_and_comprehensive`, declared once as an `as const` mirroring its `CHECK` constraint, pinned beside the template and taxonomy version (DR-4) and audited on change like any other field. **Expensive to retrofit** — a report table that ships without it forces a migration over live reports, which is what P-11 exists to prevent | A report carries its scope; the flag is visible to form, validation and export as one value | TODO |
| 78.2 | C1–C9 in the taxonomy registry | api+config | NFR-2 and FR-155 over 33.1: the nine disclosures as **rows in the config store**, element names verbatim from the published EFRAG standard because NFR-2 makes them the schema's vocabulary rather than labels over it. Labels resolve as catalogue keys like B's (33.2), and **Romanian and Russian C labels are platform-authored with no EFRAG standing** exactly as T-14, amended 31 Aug 2026, says of B's | C1–C9 registered as data; a report pinned to a Comprehensive taxonomy version resolves all nine | TODO |
| 78.3 | Adding Comprehensive to a report in progress | api | UC-192 (FR-177) — the state `EasyESG Reporting Screens` draws. Raising scope mid-report must preserve every B answer, re-run applicability rather than reset it, and record the change (FR-54); **lowering scope is the harder half** and must not silently discard C answers | Scope raised on a live report with no B data lost; the change audited; the lowering path decided and recorded | TODO |
| **79** | **C1–C9 disclosure forms** | web | **Follows task 36.** The nine Comprehensive disclosures on 36.1's field anatomy, shown when 78.1's flag says so (UC-183 … UC-191, FR-177) | The nine modules authorable in three locales; expansion harness and axe scan pass | TODO |
| 79.1 | C1 — strategy: business model and sustainability-related initiatives | web | UC-183. Narrative-dominant and the module a bank reads first; extends B2's ground rather than repeating it | C1 complete and stored in three locales | TODO |
| 79.2 | C2 — practices, policies and future initiatives for the transition | web | UC-184, additive over B2 (36.3) — the same anatomy, a wider question | C2 complete and stored | TODO |
| 79.3 | C3 — GHG reduction targets and climate transition | web | UC-185. Targets against a **base year**, which is a calendar-date question and not an instant (NFR-34), and which reads 34.3's prior-period path to show progress | C3 complete; a target and its base year stored and comparable | TODO |
| 79.4 | C4 — climate risks | web | UC-186 — physical and transition risks with the response to each | C4 complete and stored | TODO |
| 79.5 | C5 — additional workforce characteristics | web | UC-187, extending B8 (36.9). The breakdowns share B8's table anatomy, so a second table component here is a defect rather than a need | C5 complete, on B8's table anatomy | TODO |
| 79.6 | C6 — human rights policies and processes | web | UC-188 — policies and due-diligence processes covering own workforce | C6 complete and stored | TODO |
| 79.7 | C7 — severe negative human rights incidents | web | UC-189, where **absence is a positive statement** rather than an empty field — the same distinction 36.12 draws for B11, on 36.13's terminal states | C7 complete; a nil return distinguishable from an unanswered one | TODO |
| 79.8 | C8 — revenues from certain sectors and benchmark exclusion | web | UC-190 — the sector-exclusion question a bank's benchmark screening asks | C8 complete and stored | TODO |
| 79.9 | C9 — gender diversity ratio in the governance body | web | UC-191. A computed ratio beside its inputs, like B9's accident rate (36.10), so the derivation is visible rather than asserted | C9 complete; the ratio derived and traceable | TODO |
| **80** | **Comprehensive validation and export** | api+worker | **Follows tasks 41 and 46.** C1–C9 through the mechanisms B1–B11 already uses, driven by 78.1's flag | A Comprehensive report validates and exports in both formats, by one mechanism | TODO |
| 80.1 | Conditional applicability for C | api+config | C's own thresholds as rule data over 41.1 — the same interpreter, more rows. **The prototype's claim is the acceptance test:** `Organization Admin` states Comprehensive disclosures "stay hidden until a value crosses a threshold, and then you are told which report it affects" | A threshold crossing reveals its C section and says which report it affects | TODO |
| 80.2 | Validation and roll-up over C | api+web | 41.2, 41.3 and 42.2 extended by data rather than by code; a Basic report's verdicts must not change because C exists | C findings recorded and rolled up; a Basic report's verdicts are byte-identical before and after | TODO |
| 80.3 | The export treatment | worker | `Reporting Core` §7.13: **"Basic and Comprehensive" on the cover and in the identity line**, "because a reader who asked for Comprehensive needs to see they got it". Both formats, over 44.3 and 46.1, and the EFRAG template's own C sheets (45.2) | Both exports state the scope on the cover and populate C; the golden corpus (46.4) gains a Comprehensive report | TODO |
| **81** | **The Basic-and-Comprehensive plan scope** | api+web+admin | **Follows tasks 53, 54, 63 and 67.** The commercial half of OQ-12: Comprehensive as a sellable scope, which is what the `Commerce` prototype draws | Scope is an entitlement, upgradable mid-cycle, priced and editable as data | TODO |
| 81.1 | Scope as an entitlement | api+config | 53.2's grants gain report scope, and 54.2's seams gain the check. **`BILLING_ENABLED=false` must still author Comprehensive** (NFR-1, AD-5's null implementation grants everything) — otherwise the billing boundary has silently become a reporting-core dependency, which is exactly what DR-1 forbids | Scope gated by entitlement with billing on; fully available with billing off | TODO |
| 81.2 | Upgrade mid-cycle | api+web | The `Commerce` artboard's own case: *"Basic → Basic and Comprehensive"*, with unused days credited. Proration over 58.1's saga and 61's invoicing; the upgrade raises 78.3's flag on existing reports | An upgrade e2e with credit applied and reports switched to the wider scope | TODO |
| 81.3 | The scope tier in the console | admin | 68.2's A-09 gains scope as a plan dimension, and 67.6's A-04 gains the Comprehensive taxonomy version the `Admin Console` artboard draws ("VSME 2026 — Comprehensive") | Scope priced and versioned from the console with no redeploy | TODO |
| **82** | **UX-80's dark scheme, across the cascade** | ui | **Appended 26 Aug 2026** (project owner), on task 26.4's component review, and it closes `design_spec.md` **OQ-14**'s scheduling half — that register row has carried this since 18 Aug 2026 and is cross-logged to this task. UX-80 is closed and normative — *"Every semantic token shall be defined for both light and dark schemes and shall satisfy §10.2 contrast in both… whether or not the toggle ships at MVP"* — and `packages/ui/src/styles/tokens.css` defines no dark map at all, so this is an **unmet requirement**, not an open question. Tier 1 and tier 2 work spanning every semantic token, deliberately not folded into a screen task: a component task that also authored a second palette would bury the contrast verification that is the actual deliverable. Taken now rather than later because UX-80 names its own failure mode — *"retrofitting it after twenty screens is the expensive path"* — and the count is four live screens | Every semantic token resolves in both schemes; §10.2 contrast verified in both and recorded; no component file defines a colour outside the cascade | TODO |
| **83** | **The organization switcher** | api+web | **Appended 29 Aug 2026** (project owner), split out of task 30.1 rather than built inside it. `design_spec.md` OQ-6 gives the *switch* half of UC-16 (FR-12) to the global-tier switcher, and nothing can perform it: `identity.session.active_organization_id` has three writers — 25.4's post-sign-in branch, 26.2's acceptance, 29.1's founding — and **no route a user can call**, which is why 29.1's own build-log entry called the switcher "the only way to state a preference" while it was still TODO. **The route is `PUT /api/v1/session/organization`**, decided with the split: it names the thing that changes, it is idempotent, and it stays off the unauthenticated `/auth` surface. Validated against the caller's own active memberships — `membership_self_select` already grants that read anywhere — and `identity.session` carries no RLS, so `account_id` in the predicate is the only thing scoping the write, exactly as `setActiveOrganization` records at its two existing call sites. Task 30.1 shipped the read half: `GET /memberships` answers `active`, so the switcher's current-row marker and the band's name are already the guard's own resolution. **Blocked until this lands:** an account holding several memberships with no stated preference has no active organization at all, so every `@RequiresRole` route refuses it — the state `selectActiveMembership` returns null for, by design (UX-2) | The active organization changed from any screen; the band names the new one on the next render; UX-3's return-to-home honoured and its equivalent-screen clause decided | TODO |
| 83.1 | The switch route | api | `PUT /api/v1/session/organization` behind `@RequiresAccount()`, with its line in `src/testing/route-permissions.ts` and its cell in the route matrix. A membership the caller does not hold, or holds as `removed`, is refused rather than written — the failure that would otherwise put someone in a tenant they were removed from, which `selectActiveMembership` degrades away from on the read side and which the write must refuse on its own | The session points at the chosen organization; a stale or foreign id is refused with a sentence | TODO |
| 83.2 | The switcher in the global tier | web+pkg:ui | Task 30.1's organization plate becomes the trigger §11.5 names, with the artboard's open menu: the caller's organizations, the role held in each, the current one marked from `active`, and *Create another organization* as the closing entry. **UX-3 decides where the switch lands** — the equivalent screen where one exists, that organization's home otherwise — and the role differs per organization, so "equivalent" is not decidable from the URL alone; deciding it is part of this row rather than a detail. The artboard's draft-safety note ships here only if task 35.2's autosave has landed; otherwise it waits, per task 30.1's recorded deferral | The switcher chooses from any screen, in three locales; the landing rule stated and tested | TODO |
| **84** | **S-12 — field change history** | api+web | **Appended 29 Aug 2026** (project owner), found while task 30.3 traced who owns S-15's *history* link. `design_spec.md` §4.4 carries **S-12 — Field change history** with UC-47, FR-54 and FR-55, all MVP, and **no task built it**: task 14 writes `core.field_change` by trigger, task 30.3 reads one row of it for an attribution line, and nothing reads the trail itself. The same class of gap tasks 28.2 and 28.4 closed by assigning `AdminRealmGuard` and `AuditInterceptor`, and that Phase 9 closed for the public tier — a screen the inventory names, with requirements behind it, that a plan sliced from §15.4 could not contain. **Wanted by more than one screen**, which is why it is not folded into any of them: S-15's attribution, S-13's entity record, and FR-55's per-field history inside the wizard all point at it | The trail readable per record, in three locales, on the Panel archetype; every entry names the field, the actor, the moment and both values | TODO |
| 84.1 | The change-history read | api | One route over `core.field_change` scoped by table and record, paginated, `@RequiresRole` — the table is tenant-scoped and append-only, so the read is the only thing that has ever needed writing. **`actor_id` carries no foreign key** (task 14, deliberately: an attribution must survive the account it names), so resolving a person to an address is a join the read performs and must tolerate finding nobody | The trail for one record read over real HTTP; a change by a since-deleted account still names the field and the moment | TODO |
| 84.2 | S-12 the panel | web | The Panel archetype (§4.6): dismissible, non-modal, retains position. Entry from any Record field's history affordance and from S-15's attribution line. **Old and new values are user-facing text** — a `legal_form` key or a raw enum member rendered here is the defect CLAUDE.md's user-facing-text rule names, and this screen renders *every* field the trigger captures, so the resolution has to be general rather than per-field | The trail readable beside the record it describes, in three locales; no internal identifier reaches it | TODO |
| **85** | **The e2e suite times out under a full gate run** | api | **Appended 31 Aug 2026** (project owner), from four anomalies in one day — all inside a `gates`/`gates:clean` run, never in a standalone `pnpm test:e2e`, which has now passed nine times including twice at 699/699. Twice the suite was `entities.e2e-spec.ts` and once `invitation-acceptance.e2e-spec.ts` — which passed 16/16 alone and 699/699 on the very next full run, whose 25 tests take 2.9 s healthy and took 91.8 s failing: eighteen tests over jest's 5 000 ms default, a ~50× degradation starting at the 996-code classifier read. **Two hypotheses are already refuted by measurement** and should not be re-run: connection exhaustion (peak 6 of 100, never an `idle in transaction`) and orphaned servers (none older than the phase in flight). `--detectOpenHandles` over the whole suite reports **no handles and a clean exit**, so jest failing to exit is downstream of the timeouts — a timed-out test does not cancel its in-flight `supertest` request — rather than an independent leak. Unexplained: host load 6–11 during a `gates:clean` while Postgres sat at 0–24%. Also unattributed: the hung process held 18 established TCP connections to a VS Code helper, with no debugger injection in its environment. **Whether the 5 s default is the right ceiling for an HTTP suite sharing a machine with a build is part of the question and not to be answered by turning it up** — that would have made the red run green and would equally mask a real slowdown **Fifth instance, 1 Sep 2026 (task 33.2's close), with a new signature and a new suite:** `reports.e2e-spec.ts` failed one test with **`socket hang up`** inside `gates:clean`, where the four earlier instances were timeouts. The same test passed standalone in 31 s, and the whole suite passed alone at 701/701 immediately after — the pattern the row already describes. It matters because it widens the symptom set: a hung socket and a 5 000 ms timeout are the same fault only if the cause is the client side of an in-flight `supertest` request, which is what the `--detectOpenHandles` reading already suggested. The suite that fails is not stable across instances (three now: `entities`, `invitation-acceptance`, `reports`), so the trigger is the run, not the file. **Sixth and seventh instances, 1 Sep 2026 (task 34.1's close), and they break this row's central claim.** Two consecutive failures then a pass, on an unchanged tree: `invitation-acceptance` failed inside `gates:clean` with **404** on `POST /invitations`; the very next `pnpm e2e` — **standalone** — failed differently, `route-matrix` with **401** on `POST /auth/verify-email` cascading to 267 tests; the next standalone run passed **743/743**. **So "never in a standalone `pnpm test:e2e`" is now false**, and the nine standalone passes this row cites were luck rather than a property. That removes the only discriminator anyone had between "something about the full run" and "something intermittent everywhere", and it should redirect the investigation: whatever this is, it is not caused by the gate runner. The symptom set is now four — a 5 000 ms timeout, a hung exit, `socket hang up`, and now two distinct HTTP refusals from the shared `signInFreshAccount` fixture — across five suites. **One hypothesis raised and refuted here rather than left for the next reader:** leftover `identity.account` rows from a killed run do NOT explain it. They exist (two survived a `TaskStop` and a timeout that day), but re-registering an address answers **409** and the fixture expects 201, so that failure would land at registration rather than at `verify-email` four lines later. **Narrowed 1 Sep 2026 (task 88):** that reasoning is sound for `identity.account` and was never applied to `core.organization`, where a leftover is fatal rather than merely awkward — `route-matrix.e2e-spec.ts` inserted a hardcoded organization id with no pre-clean, and a seeded leftover cascades to 266 tests. It does **not** explain the instances above, whose proximate errors were a 404 and a 401 rather than a duplicate key; what it removes is the impression that leftover-row hypotheses had been disposed of as a class. **What the 401 does say** is that the fixture found an outbox row and the token inside it was rejected — consumed or expired — so the next probe is token selection under `ORDER BY occurred_at DESC`, where `now()` is transaction-stable and two rows written in one transaction sort non-deterministically. **Eighth instance, 1 Sep 2026 (task 34.2's close) — caught live and mid-hang, which separates this row's two problems and explains one of them.** `invitations.e2e-spec.ts` › *lets a different organization invite the same address* exceeded the 5 000 ms timeout inside a suite that itself took 7.9 s; 750 of 751 tests passed; jest then printed *"did not exit one second after the test run has completed"* and sat for **19 minutes at 0.0% CPU** until killed. **Measured while it hung**, which no earlier instance did: `pg_stat_activity` showed **one** connection on `esg` and that one was the diagnostic query itself, so the process held **no database connection at all**; `lsof` showed it holding exactly **one** established socket — `TCP localhost:53898->localhost:53897`, a loopback pair, which is supertest's client connection to the in-process Nest server. Only the client end survives, so the server was closed by `afterAll` and the client was not. **That is the whole of the non-exit**: one orphaned handle keeps node's event loop alive, and it is exactly the mechanism this row already suspected — *"a timed-out test does not cancel its in-flight `supertest` request"* — now with the handle identified rather than inferred. It also explains why `--detectOpenHandles` once reported a clean exit: that run had no timeout, so nothing was orphaned. **So this row is two problems and should be split.** The *hang* is understood and separately fixable — abort the pending request when a test times out, or destroy the agent in `afterAll` — and it is a test-harness defect with no product consequence. The *timeout* is still unexplained and is the one that matters. A fifth suite joins the list (`invitations`), which continues to say the trigger is the run rather than any file. **The hang half is fixed (1 Sep 2026); this row is now only about the timeout.** `apps/api/test/support/close-connections.ts` tracks every server that calls `listen` and closes its connections after each file, so an abandoned request no longer holds node's event loop. Proven by seeding: with the close removed the guard test fails *and* jest hangs, printing *"did not exit"*; with it in place both are clean — the first on-demand reproduction of this row's signature. **Not proven in situ**, because the live hang needs a timeout nobody can summon, so the next real occurrence is its test. **What remains is the 5 000 ms timeout itself, and it is unexplained.** Everything below still applies to it; what no longer applies is the non-exit, which was a consequence rather than a second symptom. **A day of investigation on the timeout eliminated five hypotheses and found none** (1 Sep 2026). Recorded so the next attempt starts where this one stopped rather than repeating it. **Ruled out by measurement:** *table bloat and autovacuum* — `core.field_change_2026` holds 575 055 rows in 125 MB on this machine, and `VACUUM (FULL, ANALYZE)` changed suite time not at all (61.4 s bloated against 62.4 / 59.9 / 60.8 s vacuumed), which also disposes of the appealing theory that CI never reproduces this because CI starts fresh; *leftover tenant rows* — four organizations survived from 28 Aug, but with zero entities, periods or reports beneath them, so they cannot explain a period-overlap 400; *the configuration store's poll* — its timer is `unref()`ed and cleared in `onApplicationShutdown`, and `refreshIfStale` short-circuits on an unchanged version, so it re-parses nothing. **Ruled out by reading:** the store's cache is rebuilt per revision, not per read. **Already ruled out previously:** connection exhaustion and orphaned servers. **One measurement worth keeping even though it explains nothing here:** `core.field_change` has *no foreign key* — it is keyed `(table_name, record_id)` — so a cascade that deletes an organization never reaches it, and on a developer machine it grows without bound. It is the whole of this database's 139 MB. Harmless for the flake; a real dev/CI divergence for anything that later scans it. **And a reframing the day's own work supports:** this row's symptoms are too varied for one cause — a 5 000 ms timeout, `socket hang up`, 400, 404, 401. Task 34.1's suite produced two of that family (**409** from an address its `beforeAll` never cleaned, **403** from enrolling a shared actor into a second organization), and both were ordinary per-suite isolation bugs with no systemic cause at all. **The non-timeout symptoms are probably several isolation defects wearing one row's name**; only the 5 000 ms timeout needs a systemic explanation. **Closed 1 Sep 2026 — it is memory pressure on this 8 GB host, and the ceiling moved to 30 s against a measured distribution rather than turned up:** `architecture.md` §12.5.6 holds the decision, `apps/api/test/README.md` the measurements, including the two CPU hypotheses it refutes. | The timeouts explained, and either fixed or shown to be an artefact of this machine; if the ceiling moves, the reason is recorded | DONE |
| **86** | **The `units` gate carries task 85's fault, and its ceilings were set for the wrong cause** | root+web+admin+pkg:ui | **Appended 1 Sep 2026**, from task 85's closing gate run, which failed `units` rather than `e2e-api`: `packages/ui` › `forms.spec.tsx` took **16 415 ms against a 15 000 ms ceiling** and passed standalone minutes later at 79/79 in 13.96 s. The three jsdom Vitest configs had raised that ceiling from 5 000 ms on **24 Aug 2026** (`c2f156e`) blaming parallel workspace runs, in three code comments and nowhere else — so task 85 opened a week later with no idea the same decision had already been taken three times. **`CLAUDE.md`'s "a rule is applied where it holds, not where it was found", with the cost attached:** the workspace it was not applied in produced eight instances over the following week. The work is to measure rather than guess — task 85's method — and to record the answer where a decision belongs | The jsdom suites' duration distribution measured idle, under the gate's concurrency and after a build; the ceiling calibrated against it rather than turned up; the cause corrected in all three comments; the decision in `architecture.md` §12.5.6 | DONE |
| **87** | **Task 85's teardown closed the connections and left the listener** | api+root | **Appended 1 Sep 2026**, from a hang caught live during task 86's closing gate run. `closeTrackedConnections()` called `server.closeAllConnections()` and nothing else; **a listening server is a ref'd handle and keeps node's event loop alive on its own**, so a file that never reached `app.close()` hangs exactly as before the fix. Task 85 could not have seen it: the instance it diagnosed had the server already closed and exactly one orphaned client, which made destroying connections look like the whole answer. The instance that found it had the opposite shape — `TCP *:53897 (LISTEN)`, six client connections, a live database session, **21 minutes at 0 % CPU**. Also here, because the hang was undiagnosable rather than merely unfixed: `gates-scoped.sh` captures each gate's output to a temp directory and summarises only on completion, so *"no output for 21 minutes"* and *"hung on the first file"* were indistinguishable with the logs being written the whole time | Both handles released, in the order that works; the listener assertion proven by seeding — remove it and the guard fails *and* jest hangs; the gate run's live output reachable while it is still running | DONE |
| **88** | **The permission matrix could report `admitted` for a request nobody admitted** | api | **Appended 1 Sep 2026**, from two defects in `route-matrix.e2e-spec.ts` observed the same day. The first is ordinary: a hardcoded organization id inserted with no pre-clean, so any run that never reaches `afterAll` makes the **next** one fail `organization_pkey` and cascade to 266 tests — and it hides, because the broken run's own `afterAll` clears the row and the run after it is clean. The second is not ordinary. The matrix reduced every response to *"one of the three guard refusals, or admitted"*, and **`internal`, `rate-limited` and `session-expired` all fell into the second arm** — so a transient 500, a throttle, or a token lapsing mid-run read as *the guards let this caller through*, on the one file whose entire purpose is to say whether they did. That is task 28.2's own recorded failure (*"read every refusal as `admitted`… it looked like the guards had stopped refusing"*) returning through a second door, and it is the likeliest reading of the `editor › PATCH /periods/:id` anomaly recorded under task 85 | Both fixed and both proven by seeding — a leftover row survived, and the classifier's inconclusive arm fails when removed; the classification a pure function with every arm under test; the decisions in `architecture.md` §12.5.6 | DONE |
| **89** | **The wizard's server half** | api | **Appended 1 Sep 2026** (project owner), found when task 35 was picked up and had nothing to talk to. The complete tenant surface is 58 routes and **not one of them serves the taxonomy or reads or writes a disclosure value** — while tasks 35.1 … 35.3 and 36.1 … 36.14 are every one `web` or `pkg:ui`, so none can add one. Task 34.1 built the store and said so in its own module: *"provided and exported with no use case above it yet: the wizard that writes through it is task 36"* — and task 36 cannot. This is the plan gap tasks 84, 67.3 and 67.4 closed in their own ranges: a deliverable sliced from §15.4 without its server half. Three surfaces, and each carries a decision rather than being incidental work: **the module read** (what a wizard step is given — elements, their kinds, dimensions and order, for the report's *pinned* version, never the newest), **the value read** (35.3's resume, which must return what was stored including FR-30/31/32's three answered-but-not-a-number states), and **the write** (35.2's autosave — per field or batched, idempotent under a flaky connection, and refused by FR-22's lock below the application as well as above it). Built before 35.1, so no screen is written against a surface that does not exist | The three surfaces live, each gated and in the route matrix; a wizard step's contents readable for a report pinned to either registered version (33.3); a value written, read back and refused inside a locked period | DONE |
| **90** | **The image's workspace-dependency list is compared to nothing** | root | **Appended 2 Sep 2026** (project owner). `apps/api/package.json` declares its `@easyesg/*` workspace dependencies and `apps/api/Dockerfile`'s runtime stage must carry a `COPY --from=build` line for each; nothing compared the two lists, and they had drifted three times (two verifiable in `git log -S`: `packages/validation` at task 20, `packages/vsme` at task 34.2 — `packages/i18n` shipped with the Dockerfile and is not one). The failure mode is what makes it worth a gate rather than a habit: a missing entry **builds cleanly** and dies at container start on a dangling symlink, so the only check that ever caught it was the Images job actually running the container — on push, after a full image build, with every other gate already green. Task 34.2's omission reached `dev` and was found four tasks later. **Both directions**, following `schema-invariants.e2e-spec.ts`'s form of declaring what is *withheld* rather than only what is granted: a copy for a package that is no longer a dependency is drift too. **Whether `apps/web` and `apps/admin` need it is part of the task and is to be confirmed rather than assumed** | A hermetic check — no Docker, no network, sub-second — failing on drift in either direction, wired into every runner that claims to be the gate set; proven by seeding; the web/admin answer recorded rather than implied; the decision in `architecture.md` §12.5.6 | DONE |
| **91** | **B1's server half** | api+config | **Appended 2 Sep 2026** (project owner), found when task 36.2 was picked up: three things it needs exist nowhere, and none can be added by a `web` task. **Enumeration members** — B1 has seven `enumeration` and three `enumeration_set` elements, and the taxonomy artefact records each element's *domain* and not one member of it, so task 35.2 rendered every one as a text box with this task named as its owner; the extractor already walks `domain-member` arcs for axes and can carry the enumeration domains the same way, two of which (`country:`, `nace:`) are published in other taxonomies and resolve from what the platform already holds. **Help text** — OQ-59, closed the same day: EFRAG's `documentation` label role extracted into the catalogues where the package carries one, official for English and platform-authored otherwise, like the labels. **Pre-population** — FR-27 and UX-109: B1's defaults from the period's entity snapshot (FR-18), served as defaults on the step read and never stored until answered, because B1 is a disclosure and the entity is *"the default, never the authority"* (§6.5). **Applicability** — FR-28: a thresholds artefact in task 16's store, evaluated by the api and answered on the module list and the step read with the B1 answer that caused it, which is what UX-27's announcement needs. Task 89's precedent: the deliverable was sliced without its server half, and each surface carries a decision | The step read answers each enumeration's members and each field's help; a B1 step opens pre-filled from the snapshot with nothing written; the module list and step read say what is applicable and why; every change proven against a report pinned to either registered version | TODO |
| 91.1 | Enumeration members and documentation labels | api+config | The extractor carries each enumeration domain's members and the `documentation` labels into the artefact and the catalogues; the step read answers `options` per enumeration field, resolved in the request's locale; the two external domains resolve from the platform's own country and activity-code data | Every B1 choice field offers its members — `vsme` domains worded in three locales, NACE named by the platform's classifier where the code maps and by EFRAG's English otherwise, the country domain as the registered countries for the client to name; help present where EFRAG publishes it and `null` on the wire where not | DONE |
| 91.2 | B1's defaults from the entity snapshot | api | The mapping from the FR-18 snapshot to B1's elements — legal form, activity codes, sites as a repeating group, consolidation basis and members — served as *defaults* on the step read, distinguishable from stored answers, and written only when the reporter answers (D-2) | A B1 step opens pre-filled; the store holds nothing until a blur; an edited default does not touch the entity | TODO |
| 91.3 | Applicability as a thresholds artefact | api+config | FR-28's four rules as effective-dated configuration in the store, evaluated against B1's stored answers on every read; the module list and the step read carry `applicable` and the causing answer; a value under a field that became inapplicable is retained, never dropped (UX-28) | A B8 turnover field appears at 50 employees and not at 49, with the cause named; the rule changes without a redeploy | TODO |
| **92** | **Inline re-authentication over the preserved wizard** | web | **Appended 2 Sep 2026** (project owner), closing OQ-60's first half. UX-38: *"Re-authentication shall be presented inline over the preserved context, never as a redirect to a blank sign-in screen"* (UC-07, FR-5). Task 35.3 proved the submission half on the existing `?return=` path; what this builds is the dialogue: an expired session met mid-wizard is re-established in place, the queue drains, and the reporter never leaves the step. Session tier and S-01's form, so it is not a wizard task | An expired session re-established without leaving S-07; the queued change submitted on success; the return-path journey's redirect assertion inverted | TODO |
| **93** | **Draft flush and warning on sign-out** | web | **Appended 2 Sep 2026** (project owner), closing OQ-60's second half. UX-37: *"warned again — with a chance to cancel — on any … sign-out … that would abandon a queue"*, and §4.1's *"log out — user menu, with draft flush per UX-37"* (UC-06). The account corner is task 30.1's surface and closed; this is the one behaviour it owes. The queue's account-scoped key (task 35.2) already keeps a left-behind queue from the next person; this makes leaving it behind a choice | Sign-out with unsent changes flushes first where it can and asks where it cannot; a clean sign-out is unchanged | TODO |
| 91.4 | Unit lists from EFRAG's measurement guidance | api+config | **Appended 2 Sep 2026** (spec review of 91.1). UX-14 requires every quantitative field to carry a unit *fixed by the taxonomy or chosen from a constrained list*, and the source of that list exists: the label linkbase carries a `measurementGuidance` role on 42 elements listing the UTR units each admits (`[utr:kg,utr:t]`). Task 35.2 deferred the chooser to *"the module that first needs one"* and 91.1 found the data and left it — twice deferred to no owner, which this row ends. **What holds meanwhile**: numeric fields carry no unit chooser and store `unit_code` null unless a module sets it | The admitted units per element in the taxonomy artefact, served on the step read; a unit chooser offered only where the taxonomy admits more than one | TODO |
| **94** | **Platform-authored help for the undocumented elements** | i18n+config | **Appended 2 Sep 2026** (spec review of 91.1), closing the residue OQ-59 left. UX-17 requires one or two sentences of help on every field; EFRAG documents 22 of 143 and task 91.1 shows those. The other 121 need platform-authored sentences in three locales (NFR-23: authored, never machine-translated, editorial sign-off per locale), in the same `help/` catalogues, with `platform_authored` standing carried so UX-47 can say so. Content work, so it is its own row rather than a clause on a screen task | Every element carries help in three locales; standing stated per entry; NFR-23's sign-off recorded | TODO |
