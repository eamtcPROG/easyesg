/**
 * S-16 — Users & access · OA · UC-59…64, UC-175 · Index
 *
 * Answers "who can see our ESG data" from one screen. Removing a user ends access but does not
 * erase their attributed contributions from the change history (UC-63, FR-55).
 *
 * Not built. `design_spec.md` §5 owns this screen's content, controls and states;
 * `design/IMPLEMENTATION_PLAN.md` owns when it lands. Prototypes in `design/screens/` are
 * the rendered reference — read them for values, never copy their markup (OQ-10).
 */
export default function UsersAndAccessPage() {
  return null;
}
