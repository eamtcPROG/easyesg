/**
 * S-13 — Entity record · OA · UC-52…55 · Record
 *
 * D-2: entity master data is OA-owned; disclosure content is RC-owned. B1 pre-populates from
 * this record (FR-27) but stays editable in-report — B1 is a disclosure, not master data.
 *
 * Not built. `design_spec.md` §5 owns this screen's content, controls and states;
 * `design/IMPLEMENTATION_PLAN.md` owns when it lands. Prototypes in `design/screens/` are
 * the rendered reference — read them for values, never copy their markup (OQ-10).
 */
export default function EntityRecordPage() {
  return null;
}
