/**
 * S-14 — Reporting periods · OA · UC-56…58 · Index + Record
 *
 * Locking makes the period read-only for RC; reopening records acting user, timestamp and a
 * stated reason (UC-57, UC-58). Each period pins its template and taxonomy version (DR-4).
 *
 * Not built. `design_spec.md` §5 owns this screen's content, controls and states;
 * `design/IMPLEMENTATION_PLAN.md` owns when it lands. Prototypes in `design/screens/` are
 * the rendered reference — read them for values, never copy their markup (OQ-10).
 */
export default function ReportingPeriodsPage() {
  return null;
}
