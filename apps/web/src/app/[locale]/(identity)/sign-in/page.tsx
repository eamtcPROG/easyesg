/**
 * S-01 — Sign in · CA · UC-01…05 · Focus
 *
 * Email and password alongside Google/Microsoft (D-6). UX-108 (Accessible Authentication,
 * WCAG 2.2 3.3.8) constrains this path: no cognitive function test, and password managers and
 * paste must work. Designed against it, not audited against it later (§9.1).
 *
 * Not built. `design_spec.md` §5 owns this screen's content, controls and states;
 * `design/IMPLEMENTATION_PLAN.md` owns when it lands. Prototypes in `design/screens/` are
 * the rendered reference — read them for values, never copy their markup (OQ-10).
 */
export default function SignInPage() {
  return null;
}
