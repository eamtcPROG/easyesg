/**
 * S-26's namespace, declared once (task 50.2.1): the heading, the list, the states and the route's `loading.tsx`
 * read it.
 *
 * **In `components/shared/` on one test: is it read by more than one sibling?** Every region reads it.
 */
export const CENTRE_MESSAGES = 'notifications.centre' as const;
