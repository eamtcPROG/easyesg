/**
 * S-38's namespace, declared once (task 52.2.2): the route's `generateMetadata` and loading state, the section, the
 * part and both states read it. In `components/shared/` on that folder's one admission test — read by more than one
 * sibling.
 */

export const UNSUBSCRIBE_MESSAGES = 'identity.unsubscribe';
