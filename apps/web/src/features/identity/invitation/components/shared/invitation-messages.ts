/**
 * S-03's namespace, declared once (task 134): the route's `generateMetadata`, the section and three
 * of its four states read it. In `components/shared/` on that folder's one admission test; not in
 * `tools/`, because a catalogue key is a presentation concern the components own.
 */

export const INVITATION_MESSAGES = 'identity.invitation';
