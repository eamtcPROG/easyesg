/**
 * `features/organization`
 *
 * Organization profile, identifiers, members and access.
 *
 * Mirrors `apps/api/src/modules/core/organization`. S-15, S-16.
 *
 * The organization switcher is global-tier, not part of this feature (OQ-6, closed): switching
 * session scope is a persistent global action, not a behaviour of any one screen.
 *
 * Not built. Folders are `components/ hooks/ schema/ queries/ types/`, tests colocated as
 * `*.spec.tsx`.
 */
export {};
