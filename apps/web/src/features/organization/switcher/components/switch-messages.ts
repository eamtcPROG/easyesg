/**
 * The switch's message namespace, declared once for the files that read it — the provider, the notice and
 * the corner (`shared-namespace-declared-once`; a sibling, because this `components/` holds files only).
 */
export const SWITCH_MESSAGES = 'chrome.organizationSwitcher';
