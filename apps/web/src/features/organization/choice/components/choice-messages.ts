/**
 * S-37's message namespace, declared once for the files that read it — the section and the list
 * (`shared-namespace-declared-once`; a sibling rather than a `shared/`, because this `components/`
 * holds files only).
 */
export const CHOICE_MESSAGES = 'organization.choice';
