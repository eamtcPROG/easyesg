/**
 * S-07's namespace, declared once (task 134): the step's section, the read-only banner, the route's
 * `generateMetadata` and the loading state read it. The fields region's three sub-namespaces are
 * `fields/shared/step-messages.ts`, on the same test one level down.
 */
export const WIZARD_MESSAGES = 'organization.wizard' as const;
