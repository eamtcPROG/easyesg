/**
 * S-06's namespaces, declared once (task 134): the index's section, list and loading state read the
 * first; the creation screen's section, decisions and loading state read the second.
 */
export const REPORTS_MESSAGES = 'organization.reports' as const;
export const REPORT_CREATION_MESSAGES = 'organization.reports.create' as const;
