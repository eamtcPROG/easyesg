/**
 * The creation screen's namespace, declared once (task 134): the route's `generateMetadata`, the
 * section and the decisions read it.
 */

export const REPORT_CREATION_MESSAGES = 'organization.reports.create';
