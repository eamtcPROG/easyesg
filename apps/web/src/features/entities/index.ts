/**
 * `features/entities`
 *
 * Reporting entities and reporting periods.
 *
 * Mirrors `apps/api/src/modules/core/{entity,period}`. S-13, S-14.
 *
 * D-2: this is OA-owned master data. It pre-populates B1 (FR-27) but the disclosure stays
 * editable in-report, because B1 is a disclosure and not a copy of this record.
 *
 * Not built. Folders are `components/ hooks/ schema/ queries/ types/`, tests colocated as
 * `*.spec.tsx`.
 */
export {};
