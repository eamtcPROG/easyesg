/**
 * S-27's namespace, declared once (task 52.3): the route's `generateMetadata`, the loading and unreachable states, the
 * form and its three sections read it. In `components/shared/` on that folder's one admission test — read by more than one
 * sibling.
 */

export const PROFILE_MESSAGES = 'profile';
