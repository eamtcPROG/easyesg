/**
 * S-28's namespaces, declared once (task 134's parent-close review). Each section and its body read
 * the same one, and the split that made the bodies their own files would otherwise have written
 * each literal twice.
 */
export const FACTOR_MESSAGES = 'identity.credentials.factor';
export const PROVIDERS_MESSAGES = 'identity.credentials.providers';
